# DipLock — открытые задачи

> Только то, что **ещё не сделано**. Закрыл пункт — перенёс дословно в `docs/history.md`
> (правило — `docs/rules/docs.md` п.8). Долг и разбор — `audit-2026-09.md` (A1–A11) и
> `audit.md` §7.7 (F17–F21). Дорожная карта продукта — `docs/ui/roadmap.md` §10.

## 1. P0/P1: находки живого прогона (разбор и цифры — `audit.md` §7.7)

- [ ] **F17 (P0):** `mne.fit_dipole` в MNE 1.13.2 возвращает кортеж `(dipoles, residual)` →
      точный фитинг не даёт диполей вообще. Распаковать, закрыть контракт тестом.
- [ ] **F18 (P0):** ошибки фитинга глушатся поштучно → задача `succeeded` с нулевым результатом.
      Нужен агрегат ошибок в контракте и предупреждение в UI.
- [ ] **F19 (P0):** стоимость точного фитинга — 5.4 с на точку, ≈20 ч на 130 с ЭЭГ. Оценка времени
      до запуска, превью-режим, `n_jobs`, кэш BEM и aseg, дробный прогресс.
- [ ] **F20 (P1):** flat-line ловит 222 ложных срабатывания из 290. Заменить критерий на оконный
      «почти константа» (тесты на синтетике).
- [ ] **F21 (P1):** в БД пишутся только `sessions` (`epochs`/`dipoles` пусты). Решить до Фазы 4/5:
      писать эпохи/диполи или закрепить ограничение явно. Результаты задач с этапа 6 сохраняются
      на диск (`results_dir/jobs/`) и переживают рестарт — открытым остаётся вопрос строк БД.

## 2. Рефакторинг (план — `audit-2026-09.md` §5)

- [ ] **Этап 7 (следующий срез):** A9 (`dipole_fitter` — F17/F18/F19: фитинг не возвращает диполей),
      A11 (`grid.bin` спектрограммы пересчитывается с другим `reference`, чем показанный расчёт —
      `reference` не пишется в результат задачи).

> Этап 6 (A6/A7/A8) закрыт 17.09.2026 — факт и цифры в `docs/history.md`, правила —
> `docs/rules/data-and-caches.md` п.12–14.

## 3. Отложено по решению владельца (только локальная разработка)

- [ ] **Docker:** смонтировать `~/mne_data` томом (в `.env` сейчас пути хоста), упростить `.env`
      для контейнера, добавить `HEALTHCHECK`, решить вопрос с сервисом `redis` в `docker-compose.yml`.

## 4. Дальше по продукту (статус «план» в `docs/ui/roadmap.md` §10)

- [ ] **Тестовый EDF:** проверка полного пайплайна на `data/edf/test.edf` (маркер `integration`).
- [ ] **FreeSurfer:** интеграция — расчёт BEM и `transform` на живой установке.
- [ ] **3.10+:** точный профиль расчёта (`mne.fit_dipole` на BEM), график GOF, 3D-меш (GLB + кэш),
      контур головы из `seghead.mgz`, двойной контур «белое/серое».
- [ ] **4.1+:** таблица — фильтры (GOF, амплитуда, BA/ROI), поиск, виртуализация.
- [ ] **5:** групповой анализ — read-API сессий, ORM `group_analysis`, агрегация по группам.
- [ ] **6:** CI (pytest + Vitest + сборка UI), alembic-миграции, ruff/mypy, генерация TS-типов из OpenAPI.
