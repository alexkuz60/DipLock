# DipLock — открытые задачи

> Только то, что **ещё не сделано**. Закрыл пункт — перенёс дословно в `docs/history.md`
> (правило — `docs/rules/docs.md` п.8). Долг и разбор — `audit-2026-09.md` (A1–A11) и
> `audit.md` §7.7 (F17–F21). План ниже сведён из коллективного аудита
> (`audit_strategy.md` N1–N40, `audit_strategy_gml5_3.md`, `audit_strategy_kimi3.md`,
> реконсиляция — `audit_strategy_diff.md`): все три аудита сошлись в порядке
> «доверие к числу → скорость/корректность расчёта → аппарат анализа → данные/отчёт → зрелость».

## Этап 0 (P0). Доверие к числу и наблюдаемость — ЗАКРЫТ 19.09.2026

Все пять шагов (N29, N6, N7/F20, QC-иконки каналов, N31) выполнены; факт, цифры и
известные ограничения — `docs/history.md` (19.09.2026, «Этап 0 плана»).

## Этап 1 (P1). Скорость и корректность расчёта — неделя

> Шаги 1.1 (N20), 1.2 (N15/N16) закрыты — `docs/history.md` (19.09.2026).

- [ ] **1.3 (N22 ↔ F19):** быстрый профиль диполей: coarse-to-fine сетка, `n_jobs=-1`, дефолт
      «превью по выборке эпох». Здесь же **кнопка «Уточнить для эпохи…»** (F19-точка входа): точный
      `fit_dipole` на выбранной эпохе; сетка — стартовая точка фитинга (kimi3). Уточнить при
      реализации: место кнопки (таблица локализации или вьюер эпох), контракт мини-задачи
      (`POST /recordings/{id}/dipoles` с `epoch_index` или отдельный роут), что показывать после
      уточнения (обновление строки таблицы + отметка «уточнено точным профилем»).
- [ ] **1.4 (N21):** единый источник BA-атрибуции (один объёмный атлас + признак метода в контракте):
      таблица и срез не должны расходиться. Тест: одна точка → одинаковый BA в обоих представлениях.

## Этап 2 (P1/P2). Научный аппарат анализа — 2–3 недели

- [ ] **2.1 (N8, P1):** ICA — сделать ветку достижимой (`pick` по каналам до поиска EOG, Fp1/Fp2 как
      прокси или SSP `compute_proj_eog`); либо честно отключить и задокументировать.
- [ ] **2.2 (N10, P1 + QC из 0.4):** детекторы ЭМГ/разрывов (`annotate_muscle_zscore`,
      `annotate_nan`, `annotate_break`); полный QC-светофор записи (SNR по каналам, уровень 50 Гц,
      `good_data_percent`) — расширяет иконки шага 0.4.
- [ ] **2.3 (N9, P2):** z-score → robust-оценка (медиана/MAD), устойчивость к нестационарности.
- [ ] **2.4 (N17, N18, P2):** спектр: `multitaper` для коротких эпох; лог-шкала; ERD/ERS;
      specparam/FOOOF (1/f + пики) — после IAF (1.2).
- [ ] **2.5 (N11–N14, P2):** фильтры: АЧХ-отклик в UI, гармоники notch, буферы края; IIR/Морле для
      узкой «одиночной частоты» (7.83±0.25 Гц FIR'ом не выделяется честно — kimi3).
- [ ] **2.6 (N24, N23, P2):** средний профиль локализации между `fast_grid` и BEM: eLORETA/sLORETA/
      LCMV на сфере + RIV/CI в таблице.
- [ ] **2.7 (N2, P1):** событийный режим: чтение EDF+-аннотаций/маркеров, нарезка по событиям (ERP).

## Этап 3 (P1/P2). UI и визуализация — неделя-две

- [ ] **3.1 (N32, P1):** топокарты — серверный `mne.viz.plot_topomap` (Agg, matplotlib уже в venv)
      вместо серого IDW.
- [ ] **3.2 (N27, N33, P2):** отмена серверной задачи из UI; параметры расчёта в URL.
- [ ] **3.3 (N26, P2):** дедуп `spectrum_signature`; мелкий долг клиента.
- [ ] **3.4 (остаток разрезки фронтенда):** семь SVG-слоёв `MriProjection.tsx` (723 строки) по
      правилу `docs/rules/frontend-state.md` п.6 — перенос без изменения поведения, после каждого
      шага lint/typecheck/test.
- [ ] **3.5 (N25, N35, P2):** 3D — Niivue (читает NIfTI/GIFTI/FreeSurfer как есть; реконсилированное
      решение против самописного GLB); контур головы из `seghead.mgz`, совместный курсор проекций.
- [ ] **Тестовый EDF:** проверка полного пайплайна на `data/edf/test.edf` (маркер `integration`).
- [ ] **FreeSurfer:** интеграция — расчёт BEM и `transform` на живой установке.

## Этап 4 (P0*/квартал). Данные, воспроизводимость, отчёт

- [ ] **4.1 (N38, P1):** `gamma_power` в `EpochRecord` (схема БД ≠ `freq_bands`) — последней ручной
      правкой, до alembic.
- [ ] **4.2 (N37, N39, P1):** alembic-миграции + генерация TS-типов из OpenAPI.
- [ ] **4.3 (решение владельца 17.09.2026):** `data/analytics.db` — SQLite-проекция журнала шагов
      (суммы/медианы/топ-шаги одним SQL), отдельно от транзакционной БД; вместе с alembic (4.2).
- [ ] **4.4 (N36, P0* ↔ write-API остаток F21):** таблица `recordings`, TTL-политика (записи — не
      24 ч), анонимизация PHI заголовка EDF при загрузке; результаты UI-разделов (быстрый расчёт,
      спектрограммы, точный фитинг по кнопке) → `sessions`/`epochs`/`dipoles`.
- [ ] **4.5 (оба аудита «+ функционал»):** ROI-анализ (агрегация мощностей/диполей по BA/aparc+aseg)
      → автоотчёт сессии (PDF/HTML из собираемого provenance).
- [ ] **4.6 (N40, P2):** run manifest (версии + параметры + отпечатки), квоты кэша, session bundle
      (zip), экспорт BIDS/CSV.
- [ ] **4.7 (Фаза 5):** групповой анализ — read-API сессий, ORM `group_analysis`, сравнение сессий
      (дельты мощностей/IAF, кластеризация диполей по ROI).

## Этап 5. Производительность клиента и инфраструктура — по замерам

- [ ] **P7/P8/P9 (P2):** воркер + `OffscreenCanvas` для растра спектрограммы; срез пирамиды по окну
      + IndexedDB; зоны артефактов на canvas. Переезд в воркер — по замерам `__diplockPerf.stats()`.
- [ ] **P3 (кандидат C1):** перебор сетки диполей на клиенте (WASM SIMD + воркеры). Переоценить
      ПОСЛЕ 1.1 — серверный вынос lead-field может сделать C1 ненужным.
- [ ] **Docker (отложено владельцем, локальная разработка):** монтирование `~/mne_data` томом,
      упрощение `.env`, `HEALTHCHECK`, судьба сервиса `redis` в `docker-compose.yml`.
- [ ] **integration-job в CI (остаток F12):** кэш `~/mne_data`, чтобы «зелёный» CI проверял научные
      ветки, а не только быстрые unit-тесты.

## Принципы исполнения плана

1. Жёсткий порядок: этап 0 → 1, дальше 2/3/4 параллелятся. Нельзя: 4.2 раньше 4.1; 4.7 раньше 4.4;
   C1 раньше переоценки после 1.1.
2. Каждый шаг = тест (backend → pytest, frontend → Vitest); N6/N7/N20 — регрессионные инварианты.
3. Новые кэши/хранилища → строка в `docs/data_map.md`; длинные правила → `docs/rules/`; закрытое →
   `docs/history.md`.
4. Сознательно отложено: autoreject/mne-icalabel (оценить после 2.1–2.3), Parquet/DuckDB/Zarr
   (нужны с 4.7), connectivity/PAC (после 2.7).
