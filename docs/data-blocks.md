# Кирпичики: структура данных до накопления

> **Статус:** черновик к правкам владельца (20.09.2026). Смыслы — `concept.md` (там же §11:
> перед планированием шага освежить память по §1–§3 и §6). Этот файл отвечает на другой
> вопрос: **из каких блоков собирается приложение и что лежит в каждом** — чтобы данные не
> начали копиться в неверной форме.
>
> **Структура БД сознательно отложена** (решение владельца 20.09.2026): сначала кирпичики —
> что и с какими полями, потом таблицы. Физические носители сегодня описаны в
> `docs/data_map.md`; когда у кирпича появится носитель (кэш, файл, таблица), строка уходит
> туда.

## 1. Зачем этот слой

Кирпич описывается **до** кода: имя, что это, ключ, поля, паспорт, носитель. Код кирпича живёт
в одном сервисе (`backend/app/services/*`); «новый кирпич» = строка в реестре + правило, и
только потом расчёт. Так мы сначала договариваемся о структуре, а не переделываем её после
первых сотен записей.

## 2. Общие правила кирпичиков

1. **Паспорт обязателен.** Кирпич = паспорт (что это, чем получено, на каких параметрах) +
   значения. Числа без паспорта — не кирпич.
2. **Базис у каждого порога.** Порог несёт базис: «абсолютный» или «доля от максимума своего
   поддиапазона» (`concept.md` §3): в γ и α абсолютные числа несравнимы.
3. **КД — вердикт, а не отображение.** Составной критерий (момент ≥ X % максимума поддиапазона
   И GOF ≥ порога) — поле кирпича дипольной точки и параметр анализа: он решает, что попадает
   в кластеры и статистику.
4. **Три уровня различаются.** Измерено / модель / интерпретация — в полях и подписях, как
   требует `concept.md` §8.
5. **Идентичность кирпича.** Ключ + отпечаток параметров: один и тот же расчёт не путается с
   другим, а разные не сливаются в один.
6. **Один кирпич — один владелец в коде.** Нет владельца — нет кирпича.

## 3. Решения владельца (20.09.2026)

- **Порядок работ:** (1) согласовать реестр кирпичиков → (2) спектр (B4 + B5) → (3) дипольная
  точка и прогон по поддиапазонам (B6 + B7). Структура БД — **позже**, «совсем рано».
- **В спектральном кирпиче (B4) хранятся два дополнительных поля: «диапазон частот»** (границы
  полосы записаны явно) **и «максимум в диапазоне»** (нормировочная база).
- **Записи группируются по «эпоха + время», и «время» — это момент пика GFP** (решение владельца,
  20.09.2026): спектр привязывается к тем же моментам, что и диполи. Поэтому спектральная величина
  (B4) и дипольная точка (B6) соединяются по одному ключу — (эпоха + момент пика), а не сопоставляются
  «на глазок» по сессии.
- **Спектр считается по эпохе целиком** (решение владельца, 20.09.2026; вариант B): момент пика —
  **метка привязки** к дипольной точке, а не окно расчёта спектра. Отдельного окна «пик ± N мс»
  в B4 нет.
- **Пик GFP специфичен для поддиапазона** (наблюдение владельца, подтверждено кодом): пики
  считаются по сигналу, отфильтрованному в своей полосе (`DipoleScanParams.filter_band`, фильтр
  применяется до нарезки эпох), а длина волны у δ и γ разная — значит максимумы пиков лежат внутри
  эпохи в **разные моменты**. Следствие: пик — свойство пары **«эпоха × поддиапазон»**, и входит
  в ключ кирпичей вместе с поддиапазоном, а не как поле эпохи.
- **«Максимум в диапазоне» (B4) — два поля** (решение владельца, 20.09.2026; вариант C):
  «**полка**» — максимум мощности полосы по эпохам сессии, `band_power_max_uv2` (мкВ²), к ней
  относится X % для КД; «**пик по частоте**» — максимум PSD внутри полосы, `psd_peak_uv2_hz`
  (мкВ²/Гц) вместе с частотой пика `psd_peak_hz` (Гц — предлагаю; поправьте, если частота не нужна).
  Частота пика — готовый вход для выбора узких поддиапазонов (С3).
- **Единицы (B4):** имя поля несёт единицу — `band_power_uv2`, `psd_uv2_hz`, `relative_power`,
  `iaf_hz` (как уже принято в проекте: `amplitude_nam`, `reject_threshold_uv`). Децибелы — только
  представление (как сетка `DPS2`), не единица хранения; `None` вместо нуля, когда не измерено.
- **Набор полос (B7):** базовые пресеты из `freq_bands` (`/meta`) + полосы эксперта + «свой набор»
  с сохранением именованного набора. Набор — это **данные** (список границ) с отпечатком, а не
  хардкод в UI: новая полоса эксперта не должна требовать правки кода.
- **Канал в B4 — измерение структуры, а не обязательный выход сейчас** (решение владельца,
  20.09.2026; вариант D): числа по каналам включаются по потребности (топология, связность), сейчас
  достаточно среднего по каналам и топокарты картинкой. Среднее и любые группы (полушария, области)
  — **производные** от канального измерения, а не его замена.
- **Общий пик по широкой полосе не вводим** (по рекомендации, 20.09.2026): пик считается только
  внутри своего поддиапазона, а «насколько пики разъехались» видно прямо из B4/B6 — в момент пика
  того или иного поддиапазона. Если позже понадобится якорь эпохи, это одно число в паспорте эпохи,
  а не новое измерение.
- **Спектрограмма сессии — агрегат B5, отдельного кирпича нет** (решение владельца, 20.09.2026):
  непрерывная STFT-сетка «время × частота» остаётся представлением по одному каналу
  (`services/spectrogram.py`, контейнер `DPS2`, раздел «ЭЭГ»), а «общая картина сессии» собирается
  из рядов B5 по поддиапазонам.

## 4. Реестр кирпичиков

«Сегодня» — фактическое состояние кода, а не пожелание. Полный статус способностей —
`concept.md` §6.

| # | Кирпич | Что это | Ключ и группировка | Обязательные поля | Сегодня |
|---|---|---|---|---|---|
| B1 | Сессия (запись) | паспорт исходника и условий записи | файл (sha256) | файл, размер, sha256, аппарат, монтаж, единицы, каналы, частота дискретизации, длительность, человек (псевдоним), дата, состояние (покой/деятельность), примечание | `services/recordings.py`: реестр в RAM, 10 записей, TTL 24 ч; в БД нет |
| B2 | Подготовленный сигнал | снимок обработки до нарезки | запись + отпечаток параметров | каналы, референс, полоса фильтра, notch, окно, частота дискретизации, отпечаток | `services/prepared_signal.py` (RAM) + дисковый кэш |
| B3 | Эпоха | отрезок сигнала как единица подсчёта | запись + номер эпохи | старт, длина, артефакты (тип + порог), «принята/отброшена» | `services/epoch_segmenter.py`; пик GFP **не** поле эпохи — он зависит от поддиапазона (§3) и живёт в B4/B6; таблица `epochs` — только у legacy `/analyze` |
| B4 | Спектральная величина | числовая характеристика полосы | **поддиапазон + эпоха + момент пика GFP** в этом поддиапазоне (тот же ключ, что у B6), далее канал и диапазон частот | **диапазон частот** (границы), **два максимума:** полка по эпохам `band_power_max_uv2` (мкВ²) и пик по частоте `psd_peak_uv2_hz` (мкВ²/Гц) + `psd_peak_hz`, PSD по частотам `psd_uv2_hz`, мощность полосы `band_power_uv2` (интеграл), `relative_power`, `iaf_hz`, **окно = эпоха целиком**, паспорт метода (n_fft, число эпох, входной фильтр/notch, пик GFP этого поддиапазона) | `services/spectral.py`: в контракт уходят средние по каналам и эпохам (`psd_mean_uv2`, `power_uv2`), а канальные и по-эпохам числа считаются **внутри** (`_channel_band_power` для топокарты, `_integrate_band` для квартилей); в БД нет |
| B5 | Спектральный ряд во времени | динамика плотности/мощности по сессии; спектрограмма сессии — **агрегат этих рядов** | поддиапазон + канал (точки ряда — по эпохам, в их пиках) | время (момент пика эпохи), плотность, мощность, паспорт метода | рядами наружу нет: непрерывная STFT-сетка есть только по одному каналу (`services/spectrogram.py`, `DPS2`, раздел «ЭЭГ») |
| B6 | Дипольная точка поддиапазона | атомарный результат локализации | **поддиапазон + эпоха + момент пика GFP** в этом поддиапазоне | эпоха, время, поддиапазон, MNI x/y/z, момент (нА·м), GOF, структура, поле Бродмана, метод (`fast_grid`/BEM), **КД: прошёл/нет** и базис (X %, порог GOF, максимум момента в поддиапазоне) | `services/dipole_scanner.py` (`gfp = sqrt(mean(data²))`, `argmax` — пик уже считается), `services/dipole_fitter.py`; таблица `dipoles` — только у legacy `/analyze` |
| B7 | Прогон по набору поддиапазонов | связка «набор полос → результаты» | запись + отпечаток набора | список полос, параметры расчёта, отпечаток, статус по каждой полосе, счётчики (точек, прошедших КД, ошибок) | нет: сейчас один расчёт за раз, в сторе один `result` |
| B8 | Кластер | устойчивая единица наблюдения | прогон + поддиапазон + номер кластера | состав точек, центроид, размер, плотность, метрика и порог кластеризации, устойчивость по поддиапазонам | нет |
| B9 | Сравнение сессий | дифференциальный анализ (покой vs деятельность) | пара сессий + отпечаток | что с чем сравнивается, совпадение параметров, метрики, дельты | нет |

## 5. B4 и B5: поля (шаг 2 порядка работ)

Ключ и правила уже решены (§3): **поддиапазон + эпоха + момент пика GFP этого поддиапазона**;
окно — эпоха целиком; единицы — в имени поля; канал — измерение структуры, включается по
потребности. Колонка «сегодня» говорит, что уже есть в коде, а что предстоит.

**B4 «Спектральная величина»** — одна запись = эпоха × поддиапазон × канал.

| Поле | Тип | Единица | Сегодня |
|---|---|---|---|
| `recording_id` | str | — | есть (паспорт записи) |
| `band_lo_hz`, `band_hi_hz` | float | Гц | полосы `freq_bands` есть; «свой набор» полос — **новое** |
| `epoch_index` | int | — | есть |
| `peak_time_ms` | float | мс от начала эпохи | считается (`argmax` GFP в `services/dipole_scanner.py`), в контракт не отдаётся |
| `channel` | str | — | измерение структуры; канальные числа считаются внутри (топокарта), в контракт не отдаются (решение D) |
| `band_power_uv2` | float | мкВ² | по эпохам считается внутри (`_integrate_band` → квартили), наружу уходит агрегат |
| `band_power_max_uv2` | float | мкВ² | **новое** — «полка»: нормировочная база, к ней относится X % для КД |
| `psd_uv2_hz` | list[float] | мкВ²/Гц | наружу — `psd_mean_uv2` (среднее по каналам и эпохам); по каналам — внутри |
| `psd_peak_uv2_hz` | float | мкВ²/Гц | **новое** — максимум PSD внутри полосы |
| `psd_peak_hz` | float | Гц | **новое** — частота этого максимума (вход для выбора узких поддиапазонов) |
| `relative_power` | float | доля 0…1 | есть (N16), но считается по сессии |
| `iaf_hz` | float | Гц | есть, по сессии (не по эпохе) |
| паспорт: `reference`, `filter_band_hz`, `notch_hz`, `reject_threshold_uv`, `n_fft`, `epoch_length_ms`, `n_epochs`, `topomap_version` | — | — | есть в `SpectrumResult` |

**B5 «Спектральный ряд во времени»** — одна точка ряда = эпоха.

| Поле | Тип | Единица | Сегодня |
|---|---|---|---|
| `recording_id`, `channel`, `band_lo_hz`, `band_hi_hz` | — | — | как в B4 |
| `epoch_index` | int | — | есть |
| `peak_time_ms` | float | мс от начала эпохи | считается, в контракт не отдаётся |
| `time_ms` | float | мс от начала сессии | **новое** — ось ряда |
| `band_power_uv2` | float | мкВ² | ряд считается внутри (`series` в `epoch_stats`), наружу уходят только медиана и квартили |
| `psd_peak_hz` | float | Гц | **новое** — если ряд нужен по «горячей» частоте полосы |

Паспорт ряда — тот же, что у B4: иначе ряд и величина не сойдутся при сравнении.

**Спектрограмма сессии — агрегат B5** (решение владельца, 20.09.2026): несколько рядов по
поддиапазонам, отдельного кирпича нет. Непрерывная STFT-сетка «время × частота» остаётся
**представлением** раздела «ЭЭГ» по одному каналу (`services/spectrogram.py`, контейнер `DPS2`), а
не отдельным кирпичом.

**B6 «Дипольная точка поддиапазона»** — одна запись = поддиапазон × эпоха (точка в пике GFP этого
поддиапазона). Источник полей — `DipoleScanPointOut` (`schemas/analysis.py`).

| Поле | Тип | Единица | Сегодня |
|---|---|---|---|
| `recording_id` | str | — | есть |
| `band_lo_hz`, `band_hi_hz` | float | Гц | **новое как поле записи**: сейчас полоса — параметр задачи (`filter_band_hz`), а в БД лежит строкой `dipoles.freq_band` |
| `epoch_index` | int | — | есть |
| `peak_time_ms` | float | мс от начала эпохи | есть (`time_ms`) |
| `head_coords` | [x,y,z] | мм (система головы) | есть |
| `mni_coords` | [x,y,z] или None | мм | есть (`head_to_mni`) |
| `moment_dir` | [x,y,z] | единичный вектор | есть (`moment`) |
| `amplitude_nam` | float | нА·м | есть |
| `gof` | float | 0…1 | есть |
| `anatomical_structure` | str или None | — | есть (`aparc+aseg`) |
| `brodmann_area` | str или None | — | есть, но источников два (шаг 1.4 — свести к одному) |
| `method` | str | — | есть (`fast_grid` / `bem_fit`) |
| `kd_passed` | bool | — | **новое** — вердикт составного критерия |
| `kd_basis` | `{moment_share_x, gof_min, moment_max_nam}` | доля 0…1, 0…1, нА·м | **новое** — базис КД: X %, порог GOF, максимум момента **в этом поддиапазоне** |
| `refined` | `{method, halfwin_ms, shift_mm, grid_gof_bem, free_fit, gof}` или None | — | есть по частям (`DipoleRefineResult`); предлагаю держать уточнение **полем точки**, а не отдельной записью — «было/стало» не должно терять связь |
| паспорт задачи: `reference`, `reference_channels`, `channels`, `sfreq`, `epoch_length_ms`, `reject_threshold_uv`, `band_hz`, `notch_hz`, `grid_mm`, `n_epochs_total`, `n_epochs_used` | — | — | есть в `DipoleScanResult` |

**B7 «Прогон по набору поддиапазонов»** — одна запись = запуск по списку полос.

| Поле | Тип | Единица | Сегодня |
|---|---|---|---|
| `run_id` | str | — | **новое** (сейчас id задачи есть, набора полос — нет) |
| `recording_id` | str | — | есть |
| `bands` | list[`{band_lo_hz, band_hi_hz, label}`] | Гц | **новое** — набор полос (шаг 2: пресеты + «свой набор») |
| `params` | `{epoch_length_ms, reject_threshold_uv, grid_mm, reference, notch_hz}` | — | частично есть |
| `fingerprint` | str | — | **новое** — отпечаток набора и параметров (аналоги уже есть: `spectrum_signature`, отпечаток ассетов) |
| `per_band_status` | list[`{band_lo_hz, band_hi_hz, state, n_points, n_kd_passed, n_errors, moment_max_nam}`] | — | **новое** — включая максимум момента **по своей полосе** (база КД) |
| `created_at`, `duration_sec_calc` | — | — | частично (`duration_sec_calc` есть) |
| `warnings` | list[str] | — | есть |

**Сводки по сессии** (медиана/квартили мощности полосы, `relative_power`, `iaf_hz`, отношения
θ/β и (θ+α)/β) — производные от записей B4; общая (не канальная) сводка спектрограммы сессии —
открытый вопрос §6.

## 6. Открытые вопросы по кирпичикам

1. **Сдвиг пиков по поддиапазонам** (наблюдение владельца): если максимумы пиков действительно
   лежат в разные моменты, то сам порядок и разброс моментов по поддиапазонам внутри эпохи может
   оказаться признаком. Классификация признаков — позже (`concept.md` §9); данные для проверки
   дадут B4/B6 в текущем виде (момент пика — поле кирпича).

Решено 20.09.2026 (подробности — §3): два максимума в B4 (полка + пик по частоте), единицы в имени
поля, набор полос B7 (пресеты + «свой набор»), канал — измерение структуры с включением по
потребности, общий пик по широкой полосе — не вводим, спектрограмма — агрегат B5.

## 7. Что дальше по этому файлу

1. Реестр **B1–B9** собран (срез 20.09.2026): ключи, поля, паспорт, «сегодня»; правки владельца —
   по ходу работы.
2. Шаг 2 — **B4 + B5**: поля разложены (§5), решения по максимумам, единицам, полосам и каналу
   приняты; открытым остаётся только «сдвиг пиков как возможный признак» (§6).
3. Шаг 3 — **B6 + B7**: поля разложены (§5): дипольная точка поддиапазона (КД и базис, уточнение
   полем точки) и прогон по набору поддиапазонов с отпечатком.
4. Затем — **структура БД** под согласованные кирпичи (миграции, `todo.md` 4.2/4.4) и строки
   носителей в `docs/data_map.md`.