# UI §10–12 — дорожная карта, решения, компромиссы

## 10. Дорожная карта фаз

| Фаза | Содержание | Статус |
|---|---|---|
| **0** | Контракт API: Pydantic-схемы + `response_model` (F4), кэш меша и BA (F6), job-API с прогрессом (F7), CORS 5173 (F14), санитизация/cleanup загрузок (F10), `/meta` (F16) | ✅ 13.09.2026 |
| **1** | Каркас: рейл, тулс-хедеры, правый сайдбар, реестр разделов, тема, тултипы, Главная, Настройки, Состояние сервера, сборка в `backend/app/static/ui`, ESLint/Prettier/Vitest | ✅ 13.09.2026 |
| **2.0** | Контролы правой панели: `FieldRow`, `SegmentedControl`, `SelectField`, `NumberField`, `CheckboxRow`, `StatusPill` + тесты | ✅ 13.09.2026 |
| **2.1** | Параметры раздела EDF: zustand-срез `edfParams` (дефолты из `/meta`, признак «результат устарел»), рабочая панель EDF | ✅ 13.09.2026 |
| **2.2** | Загрузка записи: DnD/диалог с прогрессом → `POST /recordings` + карточка метаданных (`GET /recordings/{id}`); карточка убрана в срезе 2.9 — данные переехали в паспорт шапки. Дедуп по sha256 (200 + `deduplicated`) и разовая чистка накопленных копий — `backend/scripts/dedupe_recordings.py` | ✅ 13.09.2026 |
| **2.3** | Вьюер треков на фикстурах: uPlot-стек, общая ось времени, курсор, шкала мкВ, mute/solo (выпилены в срезе 2.9: вместо них клик по названию канала разворачивает трек), зум ×1…×16 (min/max-огибающая) | ✅ 13.09.2026 |
| **2.4** | Тулс-хедер раздела EDF: «Загрузить EDF», раздельные кнопки перерасчёта по стадиям, прогресс готовности, «Паспорт» сессии, комбо-бокс зума; стадии в состоянии раздела | ✅ 13.09.2026 |
| **2.5** | Реальные сигналы: read-only `GET /recordings/{id}/signals?level=` (float32-контейнер `DPS1`, ETag/304, ленивая пирамида на диске), вьюер работает с записью | ✅ 13.09.2026 |
| **2.6** | Слои результата на фикстурах: зоны артефактов, маркеры эпох, штриховка отброшенных, тултипы, легенда | ✅ 13.09.2026 |
| **2.7** | Подключение стадий: `POST /recordings/{id}/preprocess` (stage=filter/artifacts/epochs) + прогресс по этапам, кнопки шапки становятся активными | ✅ 13.09.2026 |
| **2.8** | Экспорт окна: PNG-снапшот canvas, CSV сигналов (клиентский, без запросов к серверу) | ✅ 13.09.2026 |
| **2.9** | Ревизия раздела EDF: паспорт и «Закрыть запись» в шапке (панель «Запись» убрана), навигация по окну `<<` `<` `>` `>>`, курсор по клику, разворот трека по названию канала, неотключаемый скроллбар | ✅ 14.09.2026 |
| **2.10** | Правки по итогам ручного просмотра: рабочая область в высоте окна (скроллы треков и панели появились), разметка эпох по длине нарезки результата (штриховка не «переезжает» при смене длины эпохи), ручные пометки эпох Ctrl+двойным кликом и их снятие | ✅ 14.09.2026 |
| **2.11** | Оверлеи вьюера внутри прокручиваемого контента (линия курсора на весь стек, `sticky`-подпись времени и карточка зоны), широкая полоса прокрутки (20 px), счётчик пометок по правкам, а не по ячейкам сетки | ✅ 14.09.2026 |
| **3.1** | Диполи: три SVG-проекции (силуэт головы, схема среза MNI, поля Бродмана, слой диполей), слои чекбоксами, срезы кликом и линейкой, перекрестие точки | ✅ 14.09.2026 |
| **3.2** | Диполи: реальный срез МРТ — том fsaverage (`T1` + `brainmask`) на MNI-сетке 1 мм, PNG-срез с ETag/304 (`/surface/mri`, `/surface/mri/slice/...`), слой `mri` картинкой в проекциях, реальные границы тома | ✅ 14.09.2026 |
| **3.3** | Диполи: изотропный масштаб проекций — единый мм/пиксель (`PROJECTION_SCALE`), прямоугольные фигуры (`projectionBox`), пропорциональные колонки раздела, радиологические подписи краёв (x > 0 — R) | ✅ 14.09.2026 |
| **3.4** | Диполи: быстрый расчёт по кнопке (одна точка на эпоху в пике GFP, перебор сетки ~7 мм на сферической модели), прогресс по этапам и эпохам, порог «КД ≥ X нАм», спектр δ…γ (Welch PSD) с топокартами PNG и FFT-гистограммой в выдвижной панели | ✅ 15.09.2026 |
| **3.5** | Диполи: раздельные слои позиций и векторов моментов, выделение диполя кликом с подсветкой во всех трёх проекциях, размер/плотность маркера и наконечник луча по силе момента, геометрия эллипсов Бродмана одной функцией (согласие с хит-тестом), интерактивное окно частот FFT (кнопки ритмов, «от/до», весь диапазон) | ✅ 15.09.2026 |
| **3.6** | Диполи: форма фильтров расчёта — пресеты δ…γ из `/meta`, «свой диапазон», одиночная частота (`f ± bw/2`), «без фильтра» и сетевой 50/60 Гц; полоса и notch уходят в **обе** задачи (`band_min`/`band_max`/`notch_hz`), результат на других параметрах помечается устаревшим | ✅ 15.09.2026 |
| **3.7** | Диполи: **воспроизведение траектории** — кадр по сетке эпох результата (×1 — реальное время записи, ×2/×4), интерполяция позиции и направления момента (slerp) между соседними эпохами, покадровый шаг, пауза на измеренной точке, маркер кадра с гало поверх приглушённого облака, затухающий шлейф из измеренных отрезков последних 10 с, `Space` | ✅ 16.09.2026 |
| **3.8** | Диполи: **анатомия на срезах** — контуры `aparc+aseg` (структуры) и производная объёмная разметка полей Бродмана (ribbon × `PALS_B12_Brodmann`, ближайшая вершина коры), векторные полигоны в мм MNI с ETag/304, слои `anatomy`/`brodmann` в проекциях, структура и поле под кликом | ✅ 16.09.2026 |
| **3.9** | Диполи и таблица: справка диалогом из тулс-хедера (рабочая область без пояснительных абзацев), анимация отдельным слоем `playback` (кадр, луч и шлейф не зависят от облака) с приглушением облака α 0.3, кольца позиций Ø 6 px, колонка «Структура» в таблице (метка `aparc+aseg` по MNI от сервера) | ✅ 16.09.2026 |
| **3.10+** | Диполи: точный профиль (`mne.fit_dipole` на BEM), график GOF, 3D-меш (GLB + кэш), контур головы из `seghead.mgz`, двойной контур «белое/серое» | план (бэкенд-контракт готов: 17.09.2026, этап 7 — фитинг возвращает диполи, ошибки и `warnings` в результате, прогресс по эпохам, цена в `/meta`; точка входа выбрана владельцем — **иконка «Уточнить для эпохи…»**: точный расчёт одной эпохи вместо всего файла, `todo.md` §1; далее профили `fast`/`accurate` и превью-режим) |
| **5 (ЭЭГ)** | Раздел `/eeg`: один канал — трек (огибающая пирамиды сигналов) и спектрограмма STFT по кнопке (`POST /recordings/{id}/spectrogram` → сетка `DPS2` с ETag), общий курсор и ось времени (одна полоса под треком), линейки значений справа (перетаскивание — мкВ/дел и окно частот), метки точки клика (линия частоты на спектрограмме, линия уровня на треке), нулевая линия сигнала, рамка окна трека в «обзоре», перетаскиваемый разделитель, режимы «связано/обзор», палитра/окно дБ/сглаживание как параметры просмотра, фильтр расчёта (полоса + сетевой), хоткей `3` (третий раздел рейла), переход из EDF кликом по названию канала (исправлено: захват указателя уводил `click` контейнеру), активная иконка рейла снова подсвечена (Radix `Slot` ломал `className`-функцию `NavLink`); **срез 5+**: виртуальные каналы-миксы по группам 10-20 (все каналы, полушария, лобные/височные/центральные/теменные/затылочные; группировка — `services/channel_mix.py`, состав — в паспорте записи, микс без референса) и слои артефактов из EDF на треке и спектрограмме (отбор по каналу/группе, общая легенда типов) | ✅ 17.09.2026 |
| **4** | Таблица локализации: все точки результата в таблице (семантическая `<table>`, «липкая» шапка), сортировка по номеру эпохи, состав колонок, предупреждение «результат не пересчитан» | ✅ 15.09.2026 |
| **4.1+** | Таблица: фильтры (GOF, амплитуда, BA/ROI), поиск, виртуализация (TanStack Table + Virtual), клик по строке → фокус на диполе, экспорт CSV/JSON, сохранение выборки в БД | план |
| **5** | Групповой анализ: read-API сессий, ORM `group_analysis`, агрегаты по BA, тепловая карта | план |
| **6** | CI (pytest + vitest + build) ✅ 17.09.2026, ruff/mypy ✅ 17.09.2026; осталось: alembic-миграции, генерация TS-типов из OpenAPI | частично |

## 11. Принятые решения и открытые вопросы

**Решения.**
- `backend/.env` — единственный источник конфигурации; UI показывает параметры, но не редактирует их.
- Тяжёлые статические ассеты (меш, BA-индексы) отдаются только отдельными кэшируемыми эндпоинтами,
  никогда внутри `/analyze`.
- Долгие операции — только асинхронные задачи с прогрессом; синхронный `/analyze` оставлен для скриптов.
- Предподготовка (артефакты/эпохи) — отдельный тип задачи: правка порога не должна запускать фитинг.
- **UI пишется до обработки**: сначала контролы и визуализация (на фикстурах), затем подключение
  расчёта кнопкой. Правка параметров ничего не запускает — состояние хранится отдельно от результата
  (`params` против снимка `applied`), рассинхронизация показывается как «параметры изменены».
- Параметры раздела живут в zustand-срезе раздела (`shared/state/edfParams.ts`) и сохраняются в
  localStorage; снимок результата не сохраняется — после перезагрузки страницы это честное
  «результат не рассчитан», а не «результат актуален».
- Пока задача предподготовки не подключена, кнопки перерасчёта неактивны и объясняют почему: UI не
  имитирует обработку и не запускает тяжёлый пайплайн «для проверки».
- Перерасчёт разложен на **стадии** (`filter` / `artifacts` / `epochs`), а не на одну кнопку «всё»:
  у каждой стадии свой набор параметров и свой снимок результата, поэтому правка порога артефактов
  не обесценивает уже посчитанный фильтр. Параметры отрисовки (зум, шкала мкВ, видимость зон) в
  снимок не входят — они не влияют на расчёт. Кнопки стадий живут в тулс-хедере раздела, состояние —
  в `shared/state/edfParams.ts` (`stageApplied`), срез 2.4.
- Паспорт сессии — метаданные для БД, а не для EDF-файла: правка в диалоге не перезаписывает исходник
  и не делает запросов, значения лежат в состоянии раздела и уйдут в `sessions` при запуске анализа.
- Сигналы вьюера отдаются **бинарным float32-контейнером**, а не JSON (срез 2.5): 64k точек × 18 каналов
  в JSON — это десятки мегабайт; контейнер `DPS1` (заголовок-схема в OpenAPI + канало-мажорный payload)
  отдаётся с `ETag`. Уровни пирамиды строятся **лениво при первом запросе** и кэшируются на диске:
  фоновая предсборка всех уровней при загрузке могла бы не понадобиться ни разу.
- Единицы в сигналах вьюера — всегда мкВ: контейнер не повторяет эвристику масштаба, а опирается на
  флаг `units_autoscaled` из паспорта записи (`looks_unscaled` из `edf_loader.py`).
- Кадр сигнала на клиенте (`shared/lib/signalFrame.ts`) — единый тип и для записи, и для демо-фикстуры:
  вьюер не знает, откуда данные, и не хранит сырые отсчёты ЭЭГ.
- Слои результата (зоны артефактов, границы эпох, штриховка отброшенных, срез 2.6) — **DOM поверх
  canvas**, а не часть uPlot: зона остаётся кнопкой (клик → детали, `aria-pressed`), легенда — набором
  доступных тумблеров, а зум лишь пересчитывает `left/width` через `timeToX`, не перерисовывая canvas.
  Контракт типов и цветов вынесен в `shared/lib/artifacts.ts` (реэкспорт из `edfParams`), цвета — токены
  темы (`--color-artifact-*`) с заливкой через `color-mix`, чтобы hex не дублировался в JS. Видимость
  типов — **один** параметр `artifactVisibility` для чекбоксов панели и чипов легенды: они не расходятся.
  Границы эпох считаются из параметра «длина эпохи» (геометрия), а не из результата, — сетка живая уже
  сейчас; отброшенные эпохи приходят индексом и раскладываются по текущей сетке. До нажатия кнопки
  стадии слои — детерминированная фикстура `demoLayers` с явным `source: 'demo'` (статусбар
  подписывает «демо-фикстура»), а не имитация результата; после выполнения стадии фикстуру заменяет
  результат задачи (`source: 'result'`, срез 2.7) — слоты стадий не перетирают друг друга.
- Предподготовка записи (срез 2.7) — **отдельный тип задачи** на запись, а не на загрузку: `POST
  /recordings/{id}/preprocess` (202 + `job_id`, `kind=preprocess`) возвращается сразу, прогресс идёт
  через общий `/jobs/{id}`, а результат лежит рядом с записью (`/recordings/{id}/preprocess/{job}`),
  потому что его контракт (`PreprocessResult`) отличается от `AnalyzeResponse`. Файл записи при этом
  не удаляется: он принадлежит реестру просмотра и живёт по своему TTL.
- Каждая стадия считает свой результат на **свежем** предподготовленном сигнале: параметры фильтра
  уходят в запрос любой стадии, поэтому артефакты не ищутся на сигнале со старым фильтром. Плата —
  повторное чтение/фильтрация на каждую кнопку; выигрыш — предсказуемость и независимые снимки стадий.
  **С 17.09.2026 (этап 2, A4)** повторное чтение снято кэшем подготовленного сигнала (`prepared_raw`:
  56 мс → 1 мс на `test.edf`), независимость стадий сохранена — кэш отдаёт копию и включает параметры
  фильтра в ключ (`docs/rules/data-and-caches.md` п.10).
- Снимок параметров стадии фиксируется **в момент запуска** задачи, а не по её завершении: правка
  порога, пока задача выполняется, оставляет стадию «параметры изменены» — UI не выдаёт чужой
  результат за актуальный. Детектор артефактов теперь возвращает `zones` с каналами (аннотации MNE
  их не хранят), иначе тултип слоя не смог бы показать, какие каналы затронуты.
- Стадия `filter` пока не меняет треки: пирамида сигналов (2.5) отдаёт «сырой» EDF, а перевод её на
  отфильтрованный сигнал потребовал бы отдельного кэша/ETag по параметрам фильтра. Стадия реально
  считается (читает файл, ставит референс, применяет notch и полосу) и фиксирует параметры; переход на
  filtered-пирамиду — отдельный срез, а не тихая подмена данных.
- Экспорт окна (срез 2.8) — **клиентский и без запросов**: данные уже в браузере (кадр пирамиды, слои,
  окно), сервер не пересчитывает то, что видно на экране. Геометрия и форматы живут в чистом
  `shared/lib/exportWindow.ts`, доставка файла — в `shared/lib/download.ts`; кнопки `viewer/ExportActions`
  берут окно/каналы/canvas'ы у вьюера, поэтому стоят в его полосе, а не в тулс-хедере раздела.
- CSV отдаёт **то, что видно**: строку на пару «корзина × канал» с явными `min_uv`/`max_uv`, а не
  «сырые отсчёты». Кадр уровня ×k уже прорежен сервером, и выдавать его за полноразрешённый сигнал
  нельзя — поэтому в файле нет колонки «одно число на точку», а имя файла содержит окно и уровень
  (`probe-win0.00-10.00s-level1.csv`; уровень 0 → `full`).
- PNG собирается **из canvas'ов uPlot** (по одному на трек) плюс собственные подписи каналов, шкала
  времени, зоны и сетка эпох: снапшот обязан повторять экран, а не одни линии сигнала. Canvas не читает
  CSS-токены (`color-mix` тем более), поэтому цвета разрешаются через `getComputedStyle` с hex-fallback.
  Если браузер не умеет `toBlob` (или холст не закодировался), кнопка честно показывает ошибку текстом:
  пустой файл хуже отказа.
- Vite proxy в dev вместо правки CORS (порт 5173 всё равно разрешён для прямого доступа).
- Команды из шапки в вьюер идут **через состояние с монотонным `seq`**, а не через ref/события: у
  вьюера не может быть «императивной ручки» снаружи, а `seq` отличает новую команду от повторного
  рендера и от старой команды при повторном монтировании (срез 2.9).
- Вьюер сам решает, что в нём «параметры отрисовки»: курсор, разворот трека и центр окна — локальное
  состояние `TrackStack`, в zustand живут только те настройки, что переживают перезаход в раздел
  (уровень зума, каналы, шкала). Поэтому разворот трека не сбрасывает стадии и не помечает результат
  устаревшим (срез 2.9).
- Тесты — обязательная часть среза: на момент срезов 2.2/2.3 это 87 pytest и 82 Vitest. Вьюер тестируется
  с моком uPlot (jsdom не умеет ни `matchMedia`, ни 2D-canvas): проверяются состав и порядок треков,
  окно времени, разворот трека стрелкой у названия, переход в «ЭЭГ» по названию канала, листание окна по
  команде из шапки и данные, уходящие в
  `setData`, а математика окна и огибающей — в
  `viewerMath.test.ts`. Срез 2.4 добавил 14 тестов на тулс-хедер, диалог паспорта и стор записи, срез
  2.5 — разбор бинарного контейнера (`signalFrame.test.ts`), выбор кадра под уровень и подмену
  `signal` на `SignalFrame` в тестах вьюера; контейнер в тестах собирается тем же энкодером
  (`test/signalBlob.ts`), что и в моке API. Срез 2.5 довёл счёт до 119 Vitest и 95 pytest (6 тестов
  сигналов на бэкенде
  покрывают формат, ETag/304, уровни, сохранение пика артефакта при прореживании, дисковый кэш и его
  удаление вместе с записью; плюс 2 теста разбора `SIGNAL_LEVELS` из окружения). Срез 2.6 добавил
  чистые тесты слоёв (`viewerLayers.test.ts`: детерминизм фикстуры, 2–4 зоны на тип, сетка эпох с
  хвостом и потолком, видимость, подписи) и рендер слоёв (`viewer/TrackLayers.test.tsx` +
  интеграционные тесты в `TrackStack.test.tsx`: клик по зоне → детали, тумблеры убирают зону/эпохи,
  край записи линией не помечается) — итого 155 Vitest. Срез 2.7 добавил 10 pytest на стадии
  (`test_preprocess.py`: контракт задачи и `result_url`, 400 на половину полосы и длину вне списка,
  404 чужой записи, каналы в зонах, отбраковка ровно одной эпохи всплеском, понятная ошибка при полном
  отбросе) и 12 Vitest (форма запроса по стадиям, слияние слотов слоёв, честный снимок при правке
  параметров во время задачи, ошибка стадии на кнопке, запуск кнопкой шапки) — **167 Vitest и 105
  pytest**. Срез 2.8 покрыл экспорт окна (CSV/имя файла/шкала/геометрия снапшота, ссылка и кодирование
  canvas, кнопки и текст ошибки PNG). Срез 2.9 добавил тесты ревизии: паспорт и «Закрыть запись» в шапке
  (диалог сохраняет метаданные, без записи кнопки выключены), листание окна кнопками `<<` `<` `>` `>>`
  (команда уходит в стор, окно двигается без запросов), курсор по клику (и что он не следует за мышью),
  разворот трека стрелкой у названия, переход в «ЭЭГ» по названию канала и сброс разворота при скрытии канала — **204 Vitest**, pytest без
  изменений (105): бэкенд в этом срезе не затрагивался. Срезы 3.1–3.3 добавили геометрию проекций и
  реальный срез МРТ (`mriProjections.test.ts`, `mriSlices.test.ts`, `dipoles/*.test.tsx`; backend —
  `test_png.py` и `test_mri_slices.py`, 26 тестов), срез 3.4 — спектр и быстрый расчёт
  (`spectrum.test.ts`, `dipoleCalc.test.ts`, `DipolesDrawer.test.tsx`, `DipolesToolActions.test.tsx`;
  backend — `test_spectral.py`, `test_dipole_scanner.py`), срез 4 — таблицу локализации
  (`tableRows.test.ts`, `tableParams.test.ts`, `LocalizationTableSection.test.tsx`,
  `LocalizationTablePanel.test.tsx`). Срез 3.5 — слои позиций/векторов, выделение диполя с наведением
  срезов, фиксированный маркер позиции, геометрия эллипсов Бродмана и окно частот FFT
  (`dipolePoints.test.ts`, `spectrum.test.ts`, `dipoleCalc.test.ts`, `DipolesDrawer.test.tsx`,
  `MriProjection.test.tsx`, `DipolesSection.test.tsx`). Текущий итог: **415 Vitest в 39 файлах и
  152 pytest**.

- **Маркер диполя — кольцо фиксированного размера, сила — в луче; порог «КД» ничего не подменяет**
  (срез 3.5, ✅, поправка ручной проверки): позиции — белые кольца Ø 6 px со штрихом 2 px при любом
  размере окна (пиксели делятся на масштаб SVG `renderedWidth / viewBox.width`), выделенный диполь
  залит оранжево-жёлтым; толщина лучей у всех одна (2 px), а прозрачность считается от амплитуды
  момента (`dipoleRayVisual`, насыщение на 100 нАм), поэтому облако точек читается без таблицы. Порог
  «КД ≥ X нАм» остаётся **отдельным** параметром отображения: он скрывает слабые диполи и не меняет
  вид оставшихся — иначе «скрыто порогом» и «слабее прочих» выглядели бы одинаково, и по картинке
  нельзя было бы понять, что именно скрыто.
- **Выделение диполя — просмотр, и срезы наводятся тоже** (срез 3.5, ✅, поправка ручной проверки):
  клик по точке подсвечивает её во всех трёх проекциях **и** наводит срезы на её позицию — срезы
  меняются при любом клике по фигуре, а `stopPropagation` лишь не даёт клику обработаться дважды
  (фигура навела бы срезы на «сырую» точку клика у края хит-зоны). Ничего не запрашивается у сервера.
  Выделение живёт в `dipoleCalc` рядом с результатом и сбрасывается вместе с ним (это точка
  результата, а не записи).
- **Окно частот FFT — только просмотр посчитанного** (срез 3.5, ✅): кнопки ритмов и поля «от/до»
  срезают уже полученные числа PSD, масштаб логарифма берётся по всему спектру, а окно зажимается в
  частоты текущего расчёта (оно персистится и переживает смену записи). Пересчёт спектра по-прежнему
  возможен **только кнопкой** — «фильтрация по частоте» здесь не заказ нового расчёта, а просмотр.

- **Быстрый расчёт диполей ≠ точный фитинг** (срез 3.4, ✅): раздел считает диполи «сразу», но
  другой ценой за скорость — одна точка на эпоху (пик GFP) вместо траектории по всем отсчётам,
  сферическая модель головы (σ = 0.3 См/м) вместо BEM fsaverage и перебор узлов объёмной сетки
  (≈7 мм, шаг настраивается в панели) вместо градиентной оптимизации. Ошибка позиции — порядка
  шага сетки, поэтому позиция точки кратна шагу, а не «миллиметр в миллиметр». Результат помечен
  `method='fast_grid'`, и UI показывает эту метку в шапке и в панели: выдавать быстрый режим за
  точный нельзя. Точный профиль (`mne.fit_dipole`) и playback траектории — срез 3.5+.
- **Топокарты — служебная картинка, а не публикационная** (срез 3.4, ✅): каналы монтажа
  раскладываются ортогональной проекцией на плоскость, мощность интерполируется обратными
  расстояниями, а цвет — серая шкала (PNG-энкодер проекта умеет серый + альфу). Сферическая
  сплайн-интерполяция MNE (`mne.viz.plot_topomap`) не подключена осознанно: она тянет matplotlib в
  бэкенд, а картинка нужна для формы распределения, не для публикации. Вне круга скальпа пиксель
  прозрачен: за границей электродов мощности нет, и «дорисовывать» её нельзя. Диапазон, у которого **ни
  одна измеренная частота PSD не попадает в границы**, отдаётся как `null` («не измерено») и
  подписывается «—»: `NaN` в JSON невалиден, а нулём эту ситуацию показывать нельзя. Узкая полоса
  фильтра такого не даёт: частоты PSD остаются на месте (ось — от 0 до Найквиста), и вне полосы виден
  лишь небольшой остаток фильтра — он показывается как есть, а не подменяется нулём или `null`
  (`tests/test_filter_band_jobs.py`).
- **Воспроизведение траектории показывает кадр, а не новые данные** (срез 3.7, ✅): интерполяция между
  эпохами (позиция, направление момента, амплитуда) — приём **отображения**: в результате задачи лежит
  одна измеренная точка на эпоху, и промежуточных положений никто не считал. Поэтому интерполяция идёт
  только между соседними эпохами с точками, кадр отброшенной эпохи пуст, а на паузе и при шаге кадр
  равен измеренной точке своей эпохи. В UI это подписано (подсказка кнопки и текст раздела), а `AGENTS.md`
  держит то же правило для будущих срезов. За кадром тянется **затухающий шлейф** — отрезки между
  измеренными точками соседних эпох за последние 10 секунд сессии, гаснущие с возрастом: он показывает
  путь, а не «дорисовывает» его, поэтому через разрыв (эпоху без диполя) не тянется и подчиняется порогу
  «КД» вместе с кадром. Технически часы живут **вне стора** (непрерывное время —
  локальное состояние провайдера, в zustand уходит только смена эпохи), маркер кадра обновляется
  контекстом, а статичные слои проекций не перерисовываются — иначе облако из сотен точек во всех трёх
  проекциях перерисовывалось бы 60 раз в секунду, что на 261 эпохе `test.edf` съело бы CPU.
- **Полоса расчёта и пресет — две связанные величины** (срез 3.6, ✅): в задачу уходит **полоса**
  (`band_min`/`band_max`), а δ…γ, «одиночная частота», «свой диапазон» и «без фильтра» — выбор
  пользователя, который её задаёт (`shared/lib/calcFilter.ts`). Диапазоны ритмов не дублируются в UI:
  их отдаёт `/meta` вместе с длинами эпох. Итоговая подпись печатает полосу, а не название пресета —
  иначе после смены `freq_bands` на сервере она обещала бы одни границы, а в расчёт уходили другие.
  Пресет **не выводится** из чисел для полей («свой диапазон» с границами 1–40 Гц немедленно
  «отскакивал» бы к «широкому», пряча свои поля): он хранится как выбор, а вывод из полосы
  (`filterPresetOf`) используется только как страховка при подъёме старых параметров и когда
  сохранённый ритм пропал из метаданных. Одиночная частота — узкая полоса `f ± bw/2` (как `single_freq`
  в предподготовке записи), сетевой фильтр — только 50/60 Гц, и оба правила нормализуются при любом
  вводе. Полоса живёт в сторе расчёта (`dipoleCalc`), а не берётся из параметров предподготовки записи:
  иначе правка фильтра в разделе EDF незаметно меняла бы локализацию. URL топокарты собирается из
  **результата** (`spectrumQueryOf`), а не из текущих настроек панели: картинка обязана соответствовать
  тому расчёту, чьи числа показаны рядом, иначе пользователь сравнивал бы числа одного фильтра с
  картинкой другого.
- **Спектр, диполи и порог «КД» — раздельные действия** (срез 3.4, ✅): расчёт диполей запускается
  кнопкой в шапке, спектр — своей кнопкой в выдвижной панели, а порог «КД ≥ X нАм» вообще ничего не
  запускает: это параметр отображения, он скрывает слабые диполи на проекциях (со счётчиком
  «скрыто порогом: N из M») и не меняет результат задачи. Устаревших стадий здесь нет: результат
  живёт до «Сбросить расчёт», загрузки новой записи или «Закрыть запись».

**Открытые вопросы.** Формат 3D-обмена (GLB против `.npz`) — решается в Фазе 3; профиль расчёта
по умолчанию (`fast`/`accurate`) — после появления профилей в бэкенде (F11); объём истории в UI —
зависит от read-API (Фаза 5).

## 12. Локальные компромиссы (документированные ограничения)

- **Одиночная частота задаётся полосой, а не полем `single_freq`** (срез 3.6): у задач
  `/recordings/{id}/dipoles` и `/recordings/{id}/spectrum` есть только `band_min`/`band_max`/`notch_hz`,
  поэтому форма отправляет узкую полосу `f ± bw/2` — ровно ту же, что бэкенд строит из `single_freq` в
  предподготовке записи (`bandpass_filter.py`). Числа совпадают, и в подписи видно и частоту, и
  фактические границы полосы; отдельного поля `single_freq` у этих задач пока нет (`edf_loader`
  применяет фильтр к continuous raw до нарезки, поэтому смысл фильтрации одинаковый).
- **Сетевой фильтр — только 50 и 60 Гц** (срез 3.6): список не даёт ввести произвольное значение, а
  сохранённое приводится к ближайшему из двух (`normalizeNotchHz`), чтобы состояние и список не
  разъезжались. Произвольная частота notch — отдельная задача (сервер принимает любое число).
- **Частотная сетка PSD задана сервером** (срез 3.5): окно частот FFT-графика сужает только то, что
  уже посчитано (`freqs` из результата), и не интерполирует и не досчитывает спектр на клиенте.
  Поэтому окно уже ~шага сетки может не содержать ни одной измеренной частоты — UI честно сообщает
  «в выбранном окне нет измеренных частот», а не рисует выдуманную линию. Точный произвольный
  диапазон (пересчёт PSD по своим частотам) — задача сервера и отдельный срез.
- **Поля Бродмана в проекциях — фикстура** (срез 3.1, уточнено в 3.5): эллипсы заданы таблицей
  `DEMO_BA_LAYOUT` в долях полуразмаха оси, и хит-тест проверяет попадание по **той же** геометрии,
  что нарисована (`ellipsePx` + `brodmannAreaAt`). Это согласованность клиента, а не реальные поля:
  настоящие области появятся из `PALS_B12_Brodmann` (`surface_cache.py`) — тогда форму сменит
  источник данных, а не отрисовка.
- **Только локальный сервер**: UI и API предполагаются на одной машине; авторизации в UI нет,
  сетевой доступ не предусмотрен.
- **Единицы EDF**: файлы без physical dimension MNE читает как «вольты» (в 1e6 раз больше) —
  работает авто-детект масштаба, override задаётся через `EDF_UNITS` в `backend/.env`.
- **ICA без EOG-каналов**: ICA-ветка выполняется только при наличии EOG-подобных каналов, факт
  применения фиксируется в `pipeline.ica_applied`. Для записей без EOG нужна отдельная логика (F8, P1).
- **`decim` без антиалиасинга**: перед децимацией нет low-pass фильтра (в логах MNE есть warning) —
  на GOF влияет, поэтому значение фиксируется в `pipeline.dipole_fit_decim` каждого результата.
- **Ассеты fsaverage**: при отсутствии `~/mne_data` эндпоинты `/surface*` отвечают 503 с понятным
  текстом, `/init-status` показывает `loading`/`error` — приложение не падает.
- **Ручные пометки эпох живут в браузере** (срез 2.10): Ctrl+двойной клик правит вердикт
  reject-фильтра только в UI (`edfRecording.epochMarks`, интервалы на таймлайне). В расчёт
  (нарезка эпох на сервере, фитинг диполей) эти правки пока не уходят — передача списка
  заблокированных/разрешённых интервалов в задачу будет отдельным срезом, а не тихим
  изменением уже посчитанного результата. Счётчики («ручных пометок: N» в полосе вьюера и
  «Ручных пометок: N» в панели «Эпохи») считают **пометки**, а не ячейки сетки (срез 2.11):
  после смены длины эпохи одна пометка накрывает несколько эпох новой нарезки, и счёт по ячейкам
  показывал бы больше видимых полос, чем пользователь нарисовал.
- **Изотропный масштаб проекций** (срез 3.3, ✅): раньше каждая ось растягивалась на свой размах, и
  квадрат 284×284 px показывал анатомию с искажением до 22% (в аксиальной проекции 160 мм по x и
  196 мм по y ложились в одну ширину). Теперь масштаб **один на все проекции**
  (`PROJECTION_SCALE = 1.5` px/мм — целые пиксели на размахах 160/172/196 мм), поэтому фигуры
  прямоугольные: axial 240×294, sagittal 294×258, coronal 240×258 px (`projectionBox`). Колонки
  раздела заданы **пропорционально ширине фигур** (`flex: <ширина> 1 0%`), иначе SVG растянулся бы
  по-разному и общая шкала пропала бы; свойство фиксирует тест
  `mriProjections.test.ts::держит единый масштаб` и `DipolesSection.test.tsx` (пропорции колонок).
  Сетка MNI теперь даёт **квадратные клетки** — по ним и видно, что проекция не растянута.
  Полушария подписаны по RAS (x > 0 — правое), а горизонталь аксиальной и коронарной развёрнута:
  слева на экране R, справа L (радиологическая раскладка).
- Условная схема среза (`demoSliceStructures`) остаётся запасным видом: она показывается при
  выключенном слое МРТ, а реальные структуры и поля Бродмана с **среза 3.8** приходят контурами
  атласа (слой `anatomy` и полигоны вместо эллипсов). Контур головы из тома (`seghead`) — следующий
  шаг. Границы тома — bounding box маски `brainmask`, то есть мозг занимает почти всю
  фигуру: поле подписей даёт минимальный зазор.
- **Пояснительный текст — не в рабочей области** (срез 3.9): абзац о том, как читать проекции, слои,
  расчёт и воспроизведение переехал в диалог «Справка», который открывает кнопка-иконка справа в
  тулс-хедере раздела (`sections/dipoles/DipolesHelpDialog.tsx`, разметка — как у «Паспорта» EDF).
  Рабочая область показывает фигуры и полосу состояния с числами: подсказка, занимающая пол-экрана,
  мешает смотреть данные, а читают её один-два раза за сеанс. Анимация при этом стала **отдельным
  слоем** `playback` (кадр, его луч и шлейф не зависят от слоёв облака; выключенный слой убирает
  анимацию целиком и не приглушает облако), кольца позиций — Ø 6 px, приглушение облака под кадром —
  α 0.3.
- **Таблица локализации живёт результатом задачи, а не БД** (срез 4): она показывает точки
  текущего результата `dipoleCalc` и после загрузки другой записи (или «Закрыть запись») вместе с
  ним пустеет — сохранение выборки в БД и групповые срезы это отдельные срезы. Точки без MNI
  (fsaverage недоступен) в таблице **есть** (координаты «—»): результат задачи должен быть показан
  целиком, даже если на проекции такие точки не наводятся. Структура — **отдельная колонка**
  (`anatomical_structure` из `aparc+aseg` по MNI-узлу точки): её читает сервер тем же атласом, что
  рисует контуры срезов, и «—» здесь означает «координат нет или узел не размечен», а не «нуль».
  Колонки ROI нет, пока ROI не считается:
  в `DipoleScanPointOut` есть поле Бродмана, а пустая колонка «ROI» читалась бы как «не
  определено». Сортировка пока одна — по номеру эпохи (требование среза): ключи MNI/амплитуда/GOF
  добавятся вместе с фильтрами, чтобы сортировка и фильтрация опирались на одни и те же поля.
  Порог «КД ≥ X нАм» из раздела «Диполи» — параметр отображения проекций, поэтому в таблице все
  точки: расхождение чисел между разделами объяснено подписью, а не «тихой» фильтрацией.
- **Legacy-страница**: `backend/app/static/index.html` сохранена и доступна по `/legacy` для сравнения
  с новым UI (в ней остался бесконечный поллинг `/init-status` каждые 2 с).

---

*Спецификация обновляется вместе с реализацией фаз; читается вместе с `README.md`, `AGENTS.md`
и `audit.md` (§7 — исходные находки F1–F16).*

> Часть спецификации UI (прежний `docs/ui.md` §10, §11, §12). Индекс и правила ведения — `docs/ui.md`.
> Номера разделов (§) сохранены: на них ссылаются код и тесты.

---

## Приложение: чек-лист Фазы 2 (перенесён из `todo.md`, 17.09.2026)

> Исторический план фронтенда: пункты закрыты, но список сохранён дословно как журнал
> объёма работ и требований владельца. Актуальная дорожная карта — §10 выше.

## 📋 План на следующие этапы

### Этап 2: Пользовательский UI (Frontend) — в работе

- [x] **Фаза 0 (13.09.2026):** Pydantic-схемы ответов + `response_model` (F4), кэш меша fsaverage
      и BA-меток с ETag/304 (F6), job-API с прогрессом по этапам (F7), санитизация и очистка загрузок
      (F10), CORS 5173 (F14), `GET /api/v1/meta` (F16), gzip-сжатие ассетов
- [x] **Фаза 1 (13.09.2026):** каркас UI — левый рейл разделов, тулс-хедеры, схлопываемый правый
      сайдбар, реестр разделов, тёмная тема и токены, тултипы, Главная-заставка, «Настройки»,
      «Состояние сервера» (перенос `/init-status`), заглушки EDF / Диполи / Таблица / Групповой;
      ESLint/Prettier/Vitest/typecheck; сборка в `backend/app/static/ui`
- [x] **Фаза 2, срез 2.0 (13.09.2026):** контролы правой панели — `FieldRow`, `SegmentedControl`,
      `SelectField`, `NumberField` (границы, единицы, нормализация на blur), `CheckboxRow`,
      `StatusPill` + 15 тестов
- [x] **Фаза 2, срез 2.1 (13.09.2026):** параметры раздела EDF — zustand-срез `shared/state/edfParams.ts`
      (дефолты порогов/длин эпох/каналов из `/api/v1/meta`, выбор каналов, persist только параметров)
      и рабочая панель `sections/EdfPanel.tsx`; правило «обработка только по кнопке»: правка параметров
      не делает ни одного запроса (закрыто тестом), статус результата («не рассчитан» / «параметры
      изменены» / «актуален»), кнопка «Пересчитать предподготовку» пока неактивна и объясняет почему.
      Итого 56 тестов Vitest
- [x] **Фаза 2, срез 2.2 (13.09.2026):** загрузка записи — `POST /api/v1/recordings`
      (санитизация имени, whitelist `.edf`, лимит 200 МБ, очистка при ошибке) + read-only
      `GET /api/v1/recordings/{id}`; в UI — `sections/EdfSection.tsx` (DnD и диалог, прогресс-полоса,
      карточка метаданных записи — убрана в срезе 2.9, факты о файле переехали в паспорт шапки)
      и protected-стор `shared/state/edfRecording.ts`. Загрузка возвращает
      только паспорт записи: обработка не запускается. Итого 87 тестов pytest
- [x] **Фаза 2, срез 2.3 (13.09.2026):** вьюер треков `sections/viewer/TrackStack.tsx` на uPlot —
      один чарт на канал, общая ось времени у нижнего трека, курсор с временем, шкала мкВ (общая/авто
      по каналу), mute/solo кликом по подписи канала (выпилены в срезе 2.9 — вместо них клик по
      названию канала разворачивает трек), зум колесом ×1…×16 с якорем в точке курсора,
      drag — панорама; min/max-огибающая (`shared/lib/viewerMath.ts`) сохраняет пики артефактов на
      любом зуме; демо-сигнал (`shared/lib/demoSignal.ts`) для отладки без сервера; индикатор «Зум
      треков ×N» в статусбаре. Итого 82 теста Vitest, 87 pytest
- [x] **Фаза 2, срез 2.4 (13.09.2026):** тулс-хедер раздела EDF — кнопка «Загрузить EDF» (общий
      диалог выбора файла с зоной загрузки через `fileDialogRequest`), три раздельные кнопки
      перерасчёта по стадиям (`filter` / `artifacts` / `epochs`, `sections/EdfRecalcButtons.tsx`),
      прогресс-бар готовности перерасчётов (`RecalcProgress`), кнопка «Паспорт» с диалогом
      просмотра/правки метаданных сессии (`sections/SessionPassportDialog.tsx`, данные для БД —
      EDF-файл не меняется), комбо-бокс зума отрисовки ×1…×16 (`sections/EdfZoomSelect.tsx`).
      Слой параметров переведён с одного снимка `applied` на стадии: у каждой стадии свой набор
      параметров (`STAGE_PARAM_KEYS`) и свой снимок (`stageApplied`), параметры отрисовки расчёт
      не устаревают. В панели секция «ЗАПИСЬ» — краткая инфа о файле (имя, каналы, `sfreq`, длина
      сессии) и единицы для БД вместо поясняющего текста (секция убрана в срезе 2.9: факты о файле
      и «Единицы в БД» — только в диалоге паспорта шапки). Итого 101 тест Vitest, 87 pytest
- [x] **Фаза 2, срез 2.5 (13.09.2026):** реальные сигналы записи — read-only
      `GET /api/v1/recordings/{id}/signals?level=` (`app/services/recording_signals.py`):
      ленивая пирамида ×1…×16, float32-контейнер `DPS1` (magic + JSON-заголовок + канало-мажорный
      payload min/max), ETag/304, кэш на диске в `data/cache/signals/{id}/level{k}.bin`, потоковое
      чтение EDF (`preload=False`). Вьюер переведён с демо-сигнала на кадры записи
      (`shared/lib/signalFrame.ts`: разбор контейнера, выбор кадра под уровень, `frameEnvelope`
      в `viewerMath.ts`), кадры кэшируются в сторе записи, зум не мигает пустотой — пока уровень
      грузится, показывается ближайший загруженный; `signal_levels`/`signal_base_points` в `/meta`.
      Демо-сигнал остался кнопкой отладки без сервера. Итого 119 тестов Vitest, 95 pytest
- [x] **Фаза 2, срез 2.6 (13.09.2026):** слои результата на фикстурах — зоны артефактов,
      границы эпох и штриховка отброшенных. Чистый модуль `shared/lib/viewerLayers.ts`
      (детерминированная фикстура `demoLayers` с `source: 'demo'`, сетка `buildEpochCells`
      по длине эпохи, `visibleZones`, подписи зоны); контракт типов/цветов вынесен в
      `shared/lib/artifacts.ts` (реэкспорт из `edfParams`), цвета — токены темы, заливка через
      `color-mix`. DOM-слои поверх canvas-треков `viewer/TrackLayers.tsx`: зоны-кнопки с кликом
      → панель деталей (`SelectedZoneCard`), легенда-чипы с числом зон и тумблерами (общее
      состояние с чекбоксами панели), номера эпох с порогом читаемости. `xToTime`/`timeToX`
      в `viewerMath.ts` держат слои, курсор и мышь в одной системе координат. Итого 155 тестов
      Vitest, 95 pytest
- [x] **Фаза 2, срез 2.7 (13.09.2026):** подключение стадий предподготовки. Бэкенд:
      `app/services/preprocess.py` (`run_preprocess` — стадии `filter`/`artifacts`/`epochs` на
      сохранённом файле записи), контракт `PreprocessResult`/`ArtifactZoneOut` в
      `app/schemas/analysis.py`, эндпоинты `POST /recordings/{id}/preprocess` (202 + задача через
      `job_manager`, `kind=preprocess`) и `GET /recordings/{id}/preprocess/{job_id}`; в детектор
      артефактов добавлены `zones` с каналами (аннотации MNE их не хранят), `load_edf` научился
      «без фильтра» / notch / референсу по каналам. UI: кнопки стадий активны и запускают задачу
      (`buildPreprocessForm`, `layersFromResult`, поллинг прогресса в сторе записи), результат
      заменяет демо-фикстуру слоёв (`source: 'result'`), ошибка стадии — текст в панели и точка на
      кнопке; снимок параметров фиксируется в момент запуска, поэтому правка порога во время
      задачи честно даёт «параметры изменены». Живой прогон на `data/edf/test.edf`: filter 0.09 с,
      artifacts 357 зон за 0.64 с, epochs 65 эпох (reject 120 мкВ → 3 отброшены).
      Итого 167 тестов Vitest, 105 pytest
- [x] **Фаза 2, срез 2.8 (13.09.2026):** экспорт окна — **клиентский**, без запросов к серверу.
      Чистый модуль `shared/lib/exportWindow.ts`: `windowCsv` (длинный формат
      `time_sec,channel,min_uv,max_uv` — строка на пару «корзина × канал»: кадр уровня ×k уже
      прорежен сервером, выдавать его за полноразрешённый сигнал нельзя), `exportFileName`
      (`probe-win0.00-10.00s-level1.csv`, уровень 0 → `full`, stem санитизируется), `timeTicks`
      (круглый шаг + точность подписи из шага), `snapshotLayout`/`zoneRects`/`epochMarks` и
      `drawSnapshot` (склейка canvas'ов uPlot + подписи каналов, шкала времени, зоны артефактов,
      сетка эпох, легенда; цвета — `getComputedStyle` с hex-fallback, т.к. canvas не читает токены
      темы). Доставка файла — `shared/lib/download.ts` (`downloadBlob`/`downloadText`/`canvasToBlob`,
      ошибка кодирования → текст на экране, а не пустой файл). UI: `viewer/ExportActions.tsx` (кнопки
      PNG/CSV) в полосе окна вьюера, canvas'ы треков регистрируются в `TrackStack` через
      `canvasesRef`/`registerCanvas` (ref, а не состояние — регистрация не перерисовывает вьюер).
      Кнопки выключены, когда нет видимых каналов. Итого 198 тестов Vitest, 105 pytest
- [x] **Фаза 2, срез 2.9 (14.09.2026):** ревизия раздела EDF по итогам просмотра.
      Паспорт и «Закрыть запись» переехали в шапку: панель «Запись» и карточка записи в рабочей
      области убраны (факты о файле, предупреждения и «Единицы в БД» — только в диалоге
      `SessionPassportDialog`). Вьюер: навигация по окну кнопками `<<` `<` `>` `>>`
      (команда идёт через стор записи `navRequest` с монотонным `seq` — повторное монтирование
      вьюера старую команду не применяет; при ×1 кнопки выключены), курсор ставится кликом и живёт
      до следующего клика (мышь его больше не таскает; клик после drag и по кнопкам/зонам
      игнорируется), клик по названию канала разворачивает трек на высоту видимой области
      (mute/solo выпилены), состояние разворота локально и не обесценивает стадии. Стили: утилита
      `.scroll-y-always` (нативный пропорциональный ползунок) в треках и панели опций. Тесты:
      навигация, курсор, разворот и его сброс при скрытии канала, паспорт/«Закрыть запись» в шапке.
      Итого 204 теста Vitest, 105 pytest
- [x] **Фаза 2, срез 2.10 (14.09.2026):** правки по итогам ручного просмотра вьюера (4 пункта).
      1. Каркас (`AppShell`): grid получил явную строку `grid-rows-[minmax(0,1fr)]`, колонка рабочей
      области — `min-h-0`. Неявная строка растягивалась под содержимое (main ≈ 2600 px при окне
      681 px), каркас резался `overflow-hidden`, поэтому скроллы треков и панели опций не
      появлялись вовсе, а разворот трека был незаметен (высота области = высота всего содержимого).
      Проверено в headless Chrome: main = 593 px, у треков `scrollHeight 1288 > clientHeight 393`,
      у панели 2772 > 625, развёрнутый трек занял видимую область.
      2. Разметка эпох больше не «переезжает»: сетка вьюера строится по длине нарезки **результата**
      (`layers.epochLengthMs` из `epoch_length_ms` стадии «эпохи», функция `gridEpochLength`), у
      фикстуры — по параметру панели. Индексы `rejected_epochs` раскладываются только внутри своей
      нарезки; при расхождении длин в полосе вьюера видна пилюля «разметка эпох: N мс».
      3. Ручные пометки эпох: `Ctrl`+двойной клик по треку переключает блокировку эпохи под
      курсором (`toggleEpochMark`: снять штриховку reject-фильтра или поставить свою). Пометка —
      **интервал на таймлайне** (`EpochMark`), живёт в сторе записи (`edfRecording.epochMarks`),
      видна при выключенной штриховке (акцентная штриховка/пунктир), считаются в полосе вьюера,
      снимаются кнопкой «Снять» в панели «Эпохи»; правка одной эпохи не стирает пометки соседних.
      PNG-снапшот штрихует эпохи по итоговому вердикту. В расчёт диполей пометки пока не уходят
      (`docs/ui.md` §12).
      Тесты: арифметика пометок/сетки (`viewerLayers.test.ts`), Ctrl+двойной клик и сетка результата
      (`TrackStack.test.tsx`), стор (`edfRecording.test.ts`), панель (`EdfPanel.test.tsx`), снапшот
      (`exportWindow.test.ts`), каркас (`AppShell.test.tsx`). Итого 219 тестов Vitest, 105 pytest
- [x] **Фаза 2, срез 2.11 (14.09.2026):** три замечания ручного просмотра вьюера.
      1. **Позиционер не перерисовывался при прокрутке.** Курсор (линия + подпись времени) был
      ребёнком скролл-контейнера: по CSS абсолютный потомок скроллера скроллится вместе с
      содержимым, а `inset-y-0` считается по высоте *видимой области* — при прокрутке вниз линия
      поднималась и обрывалась на середине стека (замер: до скролла `h=737, top=211`, после
      `scrollTop=400` — `top=-189, bottom=548`), подпись времени уходила за край (`top=-185`).
      Теперь оверлеи живут внутри прокручиваемого контента (`data-testid="viewer-content"`): линия
      тянется на весь стек (`h=1280` = высота контента) и едет с треками, а подпись времени и
      карточка выделенной зоны «липнут» к верху области (`sticky` в контейнере `absolute inset-0`
      высотой во весь контент). Проверено в headless Chrome: подпись остаётся на `top=219`
      (`9 px` от верха области) при `scrollTop` 300 и 551; карточка зоны — там же.
      2. **Врал счётчик ручных пометок.** Считались ячейки сетки, а панель «Эпохи» — пометки:
      после смены длины эпохи одна 2-секундная пометка накрывала две ячейки новой нарезки, и в
      полосе вьюера появлялось «ручных пометок: 6» при трёх видимых полосах (у пользователя —
      «4» при трёх). Счётчик берёт `epochMarks.length` — число правок; `hasLayers` при этом
      по-прежнему смотрит на видимые в сетке правки (`hasManualEdit`).
      3. **Узкие полосы прокрутки.** У `.scroll-y-always` было `scrollbar-width: thin` (~10 px);
      ширина задана через `::-webkit-scrollbar` — 20 px, для Firefox стандартные свойства вынесены
      в `@supports (-moz-appearance: none)` (Chrome при `scrollbar-width`/`scrollbar-color`,
      отличных от `auto`, игнорирует legacy-псевдоэлементы). Замер в headless Chrome: полоса
      `offsetWidth - clientWidth` = 22 px (20 + рамка), было 12.
      Тесты: курсор внутри контента и `sticky`-подпись, счётчик пометок против ячеек, `sticky`-карточка
      зоны в `TrackStack.test.tsx`. Итого 222 теста Vitest, 105 pytest
- [x] **Фаза 3, срез 3.1 (14.09.2026):** раздел «Диполи» — проекции мозга (геометрия, без расчёта).
      Заглушки `DipolesSection`/`DipolesPanel` заменены рабочими: три SVG-проекции (аксиальная,
      сагиттальная, коронарная) с силуэтом головы, схемой среза MNI (сетка, следы соседних срезов,
      подписи краёв L/R/A/P/S/I выводятся из знаков осей, а не из таблицы координат) и полями
      Бродмана; слой точек диполей **пуст по замыслу** — панель пишет «расчёт не подключён», фикстура
      `demoDipoleLayer` только для тестов и отладки отрисовки. Геометрия — одна на все проекции
      (`shared/lib/mriProjections.ts`): оси масштабируются независимо, `x = 0` — срединная сагитталь,
      `y = 0`/`z = 0` — через AC–PC, поэтому границы асимметричны. Клик по фигуре даёт точку **в
      плоскости своего среза** и наводит все три (`applyPointToSlices` + `snapSlice`), попадание в поле
      Бродмана считается по тем же эллипсам, что нарисованы (`brodmannAreaAt`) — второго «невидимого»
      слоя для мыши нет. Слои `head`/`mni`/`brodmann`/`dipoles` включаются чекбоксами
      (`shared/state/dipoleParams.ts`), выключенный слой не рисуется; срезы двигаются кликом (на глаз,
      с прилипанием) и `shared/ui/SliceScrubber.tsx` (точно, с маркерами `x = 0`). Раздел **ничего не
      запускает** — проверено тестом на ноль запросов к серверу. Фикстуры слоёв детерминированы (без
      ГПСЧ): картинка одинакова между рендерами, тестами и повторным монтированием. Цвета — токены
      темы `--color-mri-*` прямо в атрибутах SVG (`shared/lib/theme.ts` — для случаев, где цвет нужен
      в JS). Тесты: `dipoles/MriProjection.test.tsx` (слои, подписи краёв, сетка и следы срезов, клик →
      точка MNI и поле Бродмана, наведение), `dipoles/DipolesSection.test.tsx` (три проекции, наведение
      срезов кликом, ноль запросов), `dipoles/DipolesPanel.test.tsx` (слои, линейки срезов, сбросы),
      `mriProjections.test.ts`, `dipolePoints.test.ts`, `state/dipoleParams.test.ts`.
      Итого 267 тестов Vitest, 105 pytest
- [x] **Фаза 3, срез 3.2 (14.09.2026):** раздел «Диполи» — **реальная нарезка мозга** (том fsaverage).
      Бэкенд: `app/services/mri_slices.py` читает `mri/T1.mgz` + `mri/brainmask.mgz` и приводит их к
      MNI-выровненной сетке 1 мм (fsaverage совмещён с MNI305 — `talairach.xfm` единичная), кэширует
      том на диске (`data/cache/mri/volume-{version}.npz` + паспорт сборки) и отдаёт срез **PNG**
      (серый + альфа, вне маски прозрачно) с ETag/304; свой PNG-энкодер `app/utils/png.py` (30 строк,
      без новых зависимостей; тест `tests/test_png.py` разбирает файл обратно и ловит главную ошибку
      формата — блочное расположение каналов вместо попиксельного, из-за которого картинка
      «раздваивается»). Эндпоинты: `GET /surface/mri` (границы, шаг, плоскости, окно яркости) и
      `GET /surface/mri/slice/{plane}/{mm}.png`; `/meta` отдаёт лёгкую ссылку `mri_slices`
      (версия + базовый URL + шаг) — картинки браузер кэширует по URL, UI их не пересчитывает.
      Границы тома = bounding box маски мозга (x ±80, y −116…80, z −82…90): `MNI_BRAIN_BOUNDS` в
      `frontend/src/shared/lib/mriProjections.ts` теперь **реальные**, а совпадение с `MRI_BOUNDS`
      бэкенда проверяет `tests/test_mri_slices.py::test_geometry_matches_frontend` (константы живут в
      двух языках, поэтому расхождение ловится чтением TS-таблиц). Фронтенд: слой `mri` («Срез МРТ
      (T1)», включён по умолчанию) рисуется как SVG `<image>` под сеткой; `shared/lib/mriSlices.ts`
      квантует срез к сетке (иначе браузер кэшировал бы десять копий одного среза), собирает URL с
      версией ассета и считает прямоугольник картинки (срез накрывает всю плоскость, растяжение —
      `preserveAspectRatio="none"`; в срезе 3.3 оси получили единый масштаб, и рамка картинки стала
      прямоугольной — параметр остался как страховка). Пока срез включён,
      условная схема среза (`demoSliceStructures`) не рисуется — иначе поверх настоящей анатомии
      рисовалась бы «вторая»; недоступная картинка (503/нет тома) показывается текстом и возвращает
      схему, повтор попытки — на другом срезе (состояние хранит URL, а не флаг). Правки: 13 новых
      тестов Vitest (`mriSlices.test.ts`, слой и ошибка загрузки в `MriProjection.test.tsx`, метаданные
      в `DipolesSection.test.tsx`, слой в `DipolesPanel.test.tsx`), 27 новых pytest. Визуальная
      проверка: headless Chrome на `/ui/dipoles` — срезы через AC, на z = +30 видны тела боковых
      желудочков, на z = −50 — ствол и мозжечок.
      Итого 280 тестов Vitest, 132 pytest
- [x] **Фаза 3, срез 3.3 (14.09.2026):** диполи — **изотропный масштаб проекций**. Раньше каждая ось
      вписывалась в свой размах, и квадрат 284×284 px показывал анатомию с искажением до 22%
      (аксиальная: 160 мм по x и 196 мм по y в одной ширине). Теперь масштаб один на все проекции —
      `PROJECTION_SCALE = 1.5` px/мм (целые пиксели на размахах 160/172/196 мм), фигуры прямоугольные
      через `projectionBox`: axial 240×294, sagittal 294×258, coronal 240×258 px; масштаб в пиксели
      переводит только `normalizedToPx`/`pxToNormalized` (принимают плоскость), остальная геометрия и
      фикстуры слоёв живут в нормализованных координатах. Колонки раздела пропорциональны ширине фигур
      (`flex: <ширина> 1 0%`) — при равных колонках SVG растянулся бы по-разному, и общая шкала
      пропала бы; сетка MNI впервые даёт квадратные клетки. Подписи краёв и тултипы приведены к RAS:
      `+x` — правое полушарие (было `+x = L`), слева на экране R, справа L (радиологическая раскладка).
      Правки: 4 новых теста Vitest (`mriProjections.test.ts::держит единый масштаб` — изотропия, целые
      пиксели и точные размеры фигур; `DipolesSection.test.tsx` — пропорции колонок; тесты подписей),
      пересобраны `MriProjection.test.tsx` (клики в координатах viewBox фигуры, прямоугольник картинки),
      `mriSlices.test.ts`, `dipolePoints.test.ts`. Визуальная проверка: headless Chrome на
      `/ui/dipoles` — срезы через AC во всех трёх проекциях, круглые структуры не вытянуты.
      Итого 283 теста Vitest, 136 pytest
- [x] **Фаза 3, срез 3.4 (15.09.2026):** диполи — **быстрый расчёт по кнопке** и спектр δ…γ.
      Бэкенд: `app/services/dipole_scanner.py` — одна точка на эпоху в пике GFP (не траектория),
      перебор узлов объёмной сетки (шаг 2–20 мм, по умолчанию 7) на сферической модели головы
      (Φ = (m·r)/(4πσr³), σ = 0.3 См/м), момент А·м → нА·м (×1e9), узлы ближе 5 мм к электроду
      отбрасываются, MNI — только через `mne.read_trans` + `mne.head_to_mni`, результат помечен
      `method='fast_grid'`. Перебор векторизован (`GᵀG`, `Gᵀd`, `np.linalg.solve`), сетка кэшируется
      `lru_cache`. Замер на живой записи (`data/edf/test.edf`: 130.7 с, 500 Гц, 18 каналов, нарезка
      500 мс → 261 эпоха, все точки с MNI): шаг 7 мм — 2.2 с, 4 мм — 11.8 с, 2 мм — 99.8 с (число
      узлов растёт как шаг⁻³, поэтому шаг — главный рычаг скорости). `app/services/spectral.py` —
      Welch PSD по эпохам (reject-порог) в мкВ²/Гц (×1e12, не через `units`) и топокарты ритмов
      готовыми PNG (ортогональная проекция монтажа, IDW, серая шкала + альфа вне круга скальпа;
      matplotlib в бэкенд не тянется), дисковый кэш и ETag по `spectrum_signature`; диапазон вне
      частотной оси — `null` («не измерено»), а не `NaN` и не нуль.
      API: `POST /recordings/{id}/dipoles` и `POST …/spectrum` (kind `dipoles`/`spectrum` через общий
      `job_manager`), результат рядом с записью (`GET …/{kind}/{job_id}`), в `Job`/`JobStatus` добавлены
      `epochs_done`/`epochs_total`. UI: кнопка «Рассчитать диполи», полоса прогресса по этапам и
      эпохам, поле порога «КД ≥ X нАм», выдвижная панель (`drawer` у `AppShell`) с топокартами и
      FFT-гистограммой (логарифмическая PSD + полосы ритмов), запуск спектра — своей кнопкой.
      Порог «КД» — параметр **отображения** (`thresholdDipoleLayer` + счётчик скрытых), правка ничего
      не запускает; точки без MNI в слой проекций не попадают. Состояние — `shared/state/dipoleCalc.ts`,
      чистая логика — `shared/lib/spectrum.ts` и `shared/lib/dipolePoints.ts`.
      Тесты: +51 Vitest (41 в новых файлах `spectrum.test.ts`, `dipoleCalc.test.ts`,
      `dipoles/DipolesDrawer.test.tsx`, `dipoles/DipolesToolActions.test.tsx` и 10 в существующих —
      слой/порог в `dipolePoints.test.ts`, панель `drawer` в `AppShell.test.tsx`, метка режима и порог
      в `DipolesSection/Panel.test.tsx`) и +14 pytest (`test_spectral.py`, `test_dipole_scanner.py`:
      мощность в свой ритм, топокарта кругом с прозрачностью, ETag/304, `None` вместо `NaN`, сетка-шар,
      синтетический диполь с точностью шага, одна точка на эпоху, прогресс по эпохам, роуты).
      Итого 334 теста Vitest, 150 pytest
- [x] **Фаза 4, срез 4 (15.09.2026):** «Таблица локализации» (`/table`) — **все точки результата**
      расчёта диполей текущей записи, а не второй расчёт: раздел читает `useDipoleCalc().result` и не
      делает **ни одного** запроса (это фиксирует тест — `fetch` не вызывался), поэтому сортировка и
      состав колонок — чистая перерисовка. Семантическая `<table>` со «липкой» шапкой и скроллом
      (`app/sections/table/LocalizationTable.tsx`), состояния (нет записи / нет результата / пустой
      результат / таблица) — `LocalizationTableSection.tsx`, панель (сортировка, колонки, справка по
      результату) — `LocalizationTablePanel.tsx`. Колонки: эпоха (нумерация с 1, как в подсказках
      проекций), пик GFP внутри эпохи (с), MNI x/y/z, полушарие, амплитуда момента (нАм), GOF (%),
      поле Бродмана. Решения: точки без MNI **показываются** (координаты «—», причина — `warnings`
      задачи); `NaN`/пропуск — тоже «—», а не «0.00»; полушарие — производная величина из знака MNI x
      (RAS-раскладка), о чём сказано в подсказке колонки; колонки ROI нет, пока ROI не считается;
      порог «КД ≥ X нАм» таблицу **не фильтрует** (он параметр отображения проекций) — расхождение
      объяснено подписью; если параметры расчёта изменили после расчёта, раздел предупреждает
      «параметры изменены — результат не пересчитан» (отпечаток `calcSignature`/`resultSignature`),
      но числа не подменяет и пересчёт не запускает. Чистая логика — `shared/lib/tableRows.ts`
      (`TABLE_COLUMNS`, `localizationRows`, `sortRowsByEpoch`, `cellText`, `rowTooltip`), настройки
      просмотра — `shared/state/tableParams.ts` (персистятся; результат задачи остаётся в `dipoleCalc`).
      Тесты: +30 Vitest (`tableRows.test.ts` — все точки, «—», сортировка в обе стороны, состав и
      видимость колонок; `tableParams.test.ts` — направления, скрытие, «Показать все», неприкосновенность
      результата; `LocalizationTableSection.test.tsx` — ноль запросов, порядок строк и его переворот,
      скрытая колонка, «—», порог строки не прячет, пустой результат объяснён;
      `LocalizationTablePanel.test.tsx` — сортировка и колонки через состояние, справка, предупреждение
      о несвежих параметрах). Визуальная проверка: живая запись + реальный расчёт (120 эпох) через
      маппинг таблицы — 120 строк, порядок по эпохе в обе стороны.
      Итого 364 теста Vitest (38 файлов), 150 pytest — +81 к срезу 3.3
- [x] **Фаза 3, срез 3.5 (15.09.2026):** диполи — доработки по итогам просмотра результата.
      **Раздельные слои**: «Точки диполей» (позиции) и «Векторы моментов» — включаются порознь;
      выключение позиций не выключает лучи (карта направлений без точек осмысленна).
      **Маркер диполя** (поправка ручной проверки 16.09.2026): позиция — белое кольцо фиксированного
      экранного размера (Ø 10 px, штрих 2 px; `DIPOLE_DOT_RADIUS_PX`/`DIPOLE_DOT_STROKE_PX`, компенсация
      масштаба SVG через ResizeObserver — `renderedWidth / viewBox.width`), выделенный — с
      оранжево-жёлтой заливкой (`--color-mri-dipole`), кольцо белое (`--color-mri-dipole-point`); сила
      момента — в луче (`dipoleRayVisual`: прозрачность 0.35→1, насыщение на 100 нАм; толщина у всех
      лучей одна — `DIPOLE_RAY_STROKE_PX` = 2 px). Бегающего за мышью кружка на фигуре нет (координаты
      под курсором — текстом под фигурой).
      Наконечник вектора считается от длины луча **полигоном** (`dipoleArrowHead`,
      4…9 px + укороченный штрих): один `<marker>` на проекцию накрывал бы короткий луч целиком.
      **Выделение диполя**: клик по точке (невидимая хит-зона 9 px, `stopPropagation` от двойной
      обработки) подсвечивает её во **всех трёх** проекциях **и наводит срезы на её позицию** (срезы
      меняются при любом клике — поправка ручной проверки); повторный клик или «Снять выделение»
      снимают выбор; подпись выбора — пилюля с эпохой, MNI, полем, моментом и GOF. `selectedPointId`
      живёт в `dipoleCalc`, не персистится и сбрасывается вместе с результатом задачи.
      **Геометрия Бродмана**: эллипсы фикстур вынесены в одну функцию `ellipsePx` (отрисовка + хит-тест
      по одной формуле), согласие «нарисовано ↔ `brodmannAreaAt`» фиксирует тест в пикселях.
      **Окно частот FFT**: кнопки ритмов, поля «от/до» и «Весь диапазон» сужают график
      (`fftRangeHz` в `dipoleCalc`, персистится) — ни одного запроса; полосы вне окна приглушаются
      (`data-in-range`), но остаются на месте; масштаб логарифма берётся по всему спектру
      (`psdScale`), окно зажимается в частоты текущего расчёта (`clampFreqWindow`), пустое окно
      сообщается текстом.
      Тесты: +25 Vitest (`spectrum.test.ts` — окно: нормализация, зажим, срезка PSD, общий масштаб,
      попадание ритма; `dipolePoints.test.ts` — насыщение силы, размер/плотность, геометрия
      наконечника; `mriProjections.test.ts` — согласие эллипсов с хит-тестом; `dipoleCalc.test.ts` —
      окно без запросов, персист окна и не персист выделения, переключатель выделения;
      `MriProjection.test.tsx` — слои порознь, наконечник, размер по силе, выделение;
      `DipolesSection.test.tsx` — подсветка во всех проекциях, снятие, порог не «теряет» выделение;
      `DipolesDrawer.test.tsx` — сужение FFT по ритму и полям, пустое окно; `DipolesPanel.test.tsx` —
      отдельные чекбоксы позиций и векторов)
      Итого 389 тестов Vitest (38 файлов), 150 pytest — +25 к срезу 4
- [x] **Фаза 3, срез 3.6 (15.09.2026):** диполи — форма фильтров расчёта.
      **Полоса — то, что уходит в задачу, пресет — выбор пользователя**: список «Фильтр расчёта»
      собирается из метаданных сервера (пресеты δ…γ берут границы из `/meta.freq_bands`, как длины
      эпох), рядом «1–40 Гц (широкий)», «Одиночная частота», «Свой диапазон» и «Без фильтра», плюс
      селект сетевого фильтра (выключен / 50 / 60 Гц). Одиночная частота — узкая полоса `f ± bw/2`
      (7.83 Гц и 0.5 Гц по умолчанию, как в предподготовке записи): у задач расчёта нет поля
      `single_freq`, поэтому форма отправляет честную пару границ и печатает её в подписи.
      **Поля «своего диапазона» не «отскакивают» к пресету**, когда числа совпали (иначе полосу
      1–40 Гц было бы нельзя ввести): пресет хранится как выбор, а вывод из полосы
      (`filterPresetOf`) работает только как страховка при подъёме старых параметров и когда ритм
      пропал из метаданных. Итоговая подпись печатает **полосу из состояния** («В расчёт уйдёт:
      α — альфа 8–13 Гц · сетевой фильтр 50 Гц») — после смены конфига сервера она показывает
      фактические границы, а не числа из названия ритма. Полоса и `notch` уходят в **обе** задачи
      (`band_min`/`band_max`/`notch_hz`); правка формы ничего не запускает, а результат на других
      параметрах помечается устаревшим («параметры расчёта изменены — результат не пересчитан»).
      Нормализация (`normalizeCalcParams`) поднимает сохранённые параметры прежней версии UI, зажимает
      границы контролов и сводит пару «пресет + полоса» (пустая полоса — только у «без фильтра»);
      формат полосы в подписях — общий `filterBandText` (им же пользуется таблица локализации).
      Тесты: +25 Vitest (`shared/lib/calcFilter.test.ts` — 11: пресеты из метаданных, нормализация
      полосы, вывод пресета, `f ± bw/2`, сетевой фильтр, итог печатает полосу; `state/dipoleCalc.test.ts`
      — сеттеры и зажимы, формы обеих задач, нормализация и подъём прежних параметров;
      `dipoles/DipolesPanel.test.tsx` — ритмы из `/meta`, «свой диапазон» остаётся открытым, одиночная
      частота, «без фильтра» + сетевой, предупреждение о рассинхроне; `dipoles/DipolesToolActions.test.tsx`
      — полоса и notch действительно уходят в задачу; backend `tests/test_filter_band_jobs.py` — задачи
      спектра и диполей считают по присланной полосе и возвращают её эхом результата)
      Итого 414 тестов Vitest (39 файлов), 152 pytest — +25 Vitest и +2 pytest к срезу 3.5
- [x] **Фаза 3, срез 3.7 (16.09.2026):** диполи — **воспроизведение траектории**.
      **Кадр идёт по сетке нарезки результата**: время сессии непрерывно, эпоха — `floor(t / epoch_length_ms)`,
      длительность — `n_epochs_total × epoch_length_ms`, поэтому ×1 — реальное время записи (на
      `test.edf` 261 эпоха по 500 мс ≈ 130 с), а ускорение — ×2/×4 (другие значения приводятся к ×1).
      Внутри эпохи кадр **интерполируется** между её точкой и точкой следующей эпохи: позиция — по прямой,
      направление момента — по дуге (slerp с явными фолбэками у совпадающих и противонаправленных векторов),
      амплитуда — линейно (длина и плотность луча меняются плавно). Это **отображение, а не измерение**:
      промежуточных положений никто не считал, поэтому интерполяция идёт только между соседними эпохами с
      точками (у отброшенной эпохи кадр пуст), а на паузе и при покадровом шаге кадр равен измеренной точке
      своей эпохи. Кадр отмечается **гало** (второе кольцо акцентным цветом) при неизменном размере кольца
      позиции, за ним тянется **затухающий шлейф** — отрезки между измеренными точками соседних эпох за
      последние 10 секунд сессии (окно во времени, а не в числе эпох; `TRAIL_WINDOW_MS`,
      `TRAIL_ALPHA_HEAD`, `TRAIL_MAX_SEGMENTS`), гаснущие от кадра к хвосту; через разрыв (эпоху без
      диполя) шлейф не тянется, рисуется в слое позиций и **под** маркером, толщина — как у луча
      (`TRAIL_STROKE_PX`). Облако точек
      приглушается, выделенный кликом диполь — нет; порог «КД» действует и на кадр, и на шлейф
      (отрезок не рисуется, если слабее порога любой из его концов), но воспроизведение не останавливает.
      Конец записи — стоп на последней эпохе, play с последней
      эпохи начинает запись сначала, уход из раздела останавливает часы (кадр остаётся), повторный `Space`
      или кнопка ставит на паузу. Контролы — в тулс-хедере: play/pause, покадрово `‹`/`›`, ×1/×2/×4,
      снятие кадра, подпись «Кадр: эпоха N из M · t с · ×N»; хоткей `Space` работает только в разделе
      «Диполи» (в других разделах Space — обычная прокрутка, в полях ввода игнорируется). Внутри: команды и
      номер эпохи — в сторе (`playback` + `seekSeq`, как `navRequest.seq` в EDF), непрерывное время — в
      часах раздела (`app/sections/dipoles/playbackClock.ts`; в стор уходит только смена эпохи), маркер кадра
      получает кадр контекстом, поэтому 60 кадров в секунду обновляют только его, а срез, поля и облако из
      сотен точек не перерисовываются.
      Тесты: +47 Vitest (`shared/lib/playback.test.ts` — 17: скорости и нормализация, длительность, номер
      эпохи и доля с зажимом, пропуски отброшенных эпох, slerp с фолбэками, интерполяция позиции/вектора/
      амплитуды и «измеренные» поля кадра, подпись, шлейф: окно и затухание, разрывы, порог «КД», предел
      числа отрезков; `state/dipoleCalc.test.ts` — 9: играет/пауза с отдельным
      снятием кадра, play с последней эпохи, покадрово с зажимом и без сдвига счётчика команд, скорость,
      сброс вместе с результатом, не-персист; `dipoles/PlaybackFrame.test.tsx` — 10: часы на управляемом
      `rAF` (кадр на паузе равен измерению, ход эпох, ×2, стоп в конце записи, команда покадрово, стоп при
      уходе, порог на кадре, шлейф к кадру и его отсутствие без кадра, статичный сосед не перерисовывается);
      `dipoles/MriProjection.test.tsx` — 4:
      гало кадра и приглушение облака, затухающий шлейф под маркером, уважение выключенных слоёв; `dipoles/DipolesSection.test.tsx` — 2:
      кадр и шлейф во всех трёх проекциях без запросов, облако в обычном виде без кадра; `dipoles/DipolesToolActions.test.tsx`
      — 3: кнопки выключены без расчёта, играет/шагает/меняет скорость, подпись и снятие кадра;
      `app/layout/AppShell.test.tsx` — 1: `Space` только в разделе «Диполи»; `shared/lib/dipolePoints.test.ts`
      — 1: гало шире кольца, приглушение меньше минимальной плотности луча, толщина шлейфа как у луча)
      Итого 462 теста Vitest (41 файл), 152 pytest — +47 Vitest к срезу 3.6
- [ ] **Фаза 3, срезы 3.8+:** диполи — точный профиль (`mne.fit_dipole` на BEM), график GOF и выбор
      эпохи по GOF, 3D-меш (GLB + кэш), реальные поля
      Бродмана и контур головы из тома вместо условных фикстур
- [ ] **Фаза 4, срез 4.1+:** таблица локализации — фильтры (GOF, амплитуда, BA/ROI), поиск,
      виртуализация (TanStack Table + Virtual), клик по строке → фокус на диполе в разделе «Диполи»,
      экспорт выборки в CSV/JSON, сохранение выборки в БД, кросс-раздельная навигация
- [ ] **Фаза 5:** групповой анализ — read-API сессий, агрегаты по BA, тепловая карта

Изначальные требования к UI (сохранены как чек-лист): 3D-визуализация диполей через Three.js;
drag-and-drop загрузка EDF; форма настроек анализа (эпохи, диапазоны, пороги); 3 проекции
(axial/sagittal/coronal); анимация траектории с playback; интерактивная карта полей Бродмана;
экспорт JSON/CSV. Подробности (экраны, принципы, зум-пирамида, дорожная карта) — `docs/ui.md`.
