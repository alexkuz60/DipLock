# Инвентарь тестов: что уже покрыто

## Статические проверки: ruff + mypy + CI

Кроме тестов изменение обязано проходить два статических входа (конфиг — `backend/pyproject.toml`,
исключений «на глаз» в конфиге нет — каждое подписано причиной):

```bash
cd backend && venv/bin/ruff check app tests scripts   # линтер
cd backend && venv/bin/mypy app                        # типы (check_untyped_defs = true)
```

Набор правил ruff — осознанный, а не «всё включено»: `E4/E7/E9/F` (pycodestyle+pyflakes), `I`
(isort), `UP` (синтаксис python 3.12: `X | None`, `dict[str, int]`, `type X = …`), `B` (bugbear),
`SIM`, `RUF`, `ASYNC` (блокирующий I/O в `async def`), `S` (bandit). Исключения:

* `B008` — `Depends()`/`Query()` в значениях по умолчанию: норма FastAPI;
* `B019` — LRU-кэши на уровне модуля: осознанный приём проекта (`docs/rules/data-and-caches.md`);
* `RUF001–003` — «неоднозначные» юникод-символы: тексты проекта на русском, кириллица здесь норма;
* `ASYNC240` — `os.path`/`stat` в async-обработчиках раздачи статики: микросекундные операции,
  долгие файловые операции по-прежнему запрещены (их ловит `ASYNC230` — конвенция «Async» в `AGENTS.md`);
* per-file: `tests/**` — `S101` (assert — инструмент pytest), `tests/conftest.py` — `E402` (каталоги
  данных уводятся в tmp **до** импорта настроек), `tests/test_api_contract.py` — `S108` (тесты путей),
  `scripts/**` — `S101`/`T201` (печать — часть CLI-скрипта).

`mypy` намеренно мягкий к отсутствующим стабам (`ignore_missing_imports`, научный стек) и строгий к
телам: `check_untyped_defs = true`. Ожидаемое состояние — **0 ошибок**; «подавить» их допустимо
только точечно и с пояснением (`cast` для `nib.load`, `# noqa` для `hashlib.sha1` в ключе RAM-кэша).

CI — `.github/workflows/ci.yml`: job `backend` (`ruff` → `mypy` → `pytest -m "not integration"`) и
job `frontend` (`npm run lint` → `typecheck` → `test` → `build`). Локально те же команды, поэтому
«зелёный CI» проверяется без пуша. Инструменты лежат в `backend/requirements-dev.txt`.

**Правило:** рефакторинг без изменения поведения начинается с этой страховки — сначала зелёные
линтер/типы/тесты, потом правки, и после каждого шага прогон заново. Именно так поймано то, что
автофикс ruff удаляет импорты-реэкспорты: потребителя в тесте видно только по запуску тестов.

## Тесты

Фреймворки: **pytest** (`backend/tests/`, конфиг `backend/pytest.ini`) и **Vitest** (`frontend/src/**/*.test.tsx`).

```bash
cd backend && venv/bin/pip install -r requirements-dev.txt
cd backend && venv/bin/python -m pytest                        # все тесты backend
cd backend && venv/bin/python -m pytest tests/test_api.py -v
cd backend && venv/bin/python -m pytest -m "not integration"   # без локальных данных

cd frontend && npm run test                                    # Vitest (jsdom)
```

Тесты быстрые (без сети): синтетический ЭЭГ (`backend/tests/conftest.py`) + `TestClient`; ветки с
реальными данными (`~/mne_data`, `data/edf/test.edf`) помечаются маркером `integration` и скипаются
без них. Покрывают: контракт API и Pydantic-схемы, job-API и прогресс, кэш поверхности (ETag/304),
санитизацию и дедуп загрузок (sha256, сайдкар, 200/201), сигналы записи (формат `DPS1`, ETag/304, уровни, кэш), стадии предподготовки
(`POST /recordings/{id}/preprocess`: 202 + задача, `result_url`, 400/404, зоны с каналами, отброшенные
эпохи), config, bandpass_filter,
epoch_segmenter, montage edf_loader; на UI — каркас
(рейл, тулс-хедер, панели, хоткеи), реестр разделов, «Главная», «Настройки», «Состояние сервера»,
HTTP-клиент и разбор ошибок, контролы правой панели, параметры раздела EDF, загрузка записи
(`EdfSection`), вьюер треков (`viewer/TrackStack` — с моком uPlot, математика окна/огибающей в
`viewerMath.test.ts`, разбор контейнера сигналов в `signalFrame.test.ts`, энкодер для тестов —
`test/signalBlob.ts`) и тулс-хедер раздела (`EdfToolActions` +
`EdfRecalcButtons`, диалог паспорта `SessionPassportDialog`, стор записи `edfRecording.test.ts`),
экспорт окна (`exportWindow.test.ts` — CSV/имя файла/шкала/геометрия снапшота, `download.test.ts` —
ссылка и кодирование canvas, `viewer/ExportActions` — кнопки, содержимое файлов, ошибка PNG;
срез 2.9 добавил тесты ревизии: паспорт и «Закрыть запись» в шапке, листание окна кнопками
`<<` `<` `>` `>>`, курсор по клику, разворот трека стрелкой у названия, переход в «ЭЭГ» по названию
канала и сброс разворота при скрытии канала; срез 2.10 — скролл каркаса (классы grid в `AppShell.test.tsx`), разметку эпох по длине
нарезки результата, Ctrl+двойной клик по треку (блокировка/снятие блокировки эпохи) и чистую
арифметику пометок в `viewerLayers.test.ts`; срез 2.11 — курсор и карточка зоны внутри
прокручиваемого контента (`sticky`), счётчик пометок по правкам, а не по ячейкам сетки
(`TrackStack.test.tsx`); срез 3.1 — проекции мозга (`dipoles/MriProjection.test.tsx`: слои,
подписи краёв по знакам осей, сетка MNI и следы соседних срезов, клик → точка MNI в плоскости
среза и поле Бродмана, наведение; `dipoles/DipolesSection.test.tsx`: три проекции, наведение
срезов кликом, ноль запросов к серверу; `dipoles/DipolesPanel.test.tsx`: слои, линейки срезов,
сбросы); геометрия проекций, слой диполей и состояние раздела (`mriProjections.test.ts`,
`dipolePoints.test.ts`, `state/dipoleParams.test.ts`); срез 3.2 — реальный срез МРТ
(`shared/lib/mriSlices.test.ts`: квантование среза к сетке тома, URL с версией ассета,
прямоугольник картинки; `dipoles/MriProjection.test.tsx`: слой `mri` как `<image>`, возврат
условной схемы при выключенном слое и при ошибке загрузки; `dipoles/DipolesSection.test.tsx`:
метаданные из `/meta`, только статические запросы; `dipoles/DipolesPanel.test.tsx`: новый слой;
backend — `tests/test_png.py` (энкодер с декодером-обратным), `tests/test_mri_slices.py` —
геометрия сетки, ориентация на синтетическом томе, выборка узлов сетки из тома с настоящей
укладкой `T1.mgz`, дисковый кэш, роуты, сверка TS-констант с Python, реальный том и его пиксельная
сверка с независимым пересчётом под маркером `integration`); срез 3.4 — спектр и быстрый
расчёт диполей (`shared/lib/spectrum.test.ts`: URL топокарт с параметрами расчёта и версией
ассета, нормировка гистограммы, логарифмическая PSD, «—» вместо неизмеренной мощности;
`state/dipoleCalc.test.ts`: зажим параметров, формы задач, поллинг с прогрессом по эпохам,
раздельность ошибок расчёта и спектра, сброс; `dipoles/DipolesDrawer.test.tsx`: топокарты как `<img>`, FFT-полосы и ломаная
PSD, запуск спектра только по кнопке; `dipoles/DipolesToolActions.test.tsx`: расчёт по кнопке,
прогресс-бар, порог «КД» без запросов, одна панель за раз; `dipolePoints.test.ts`: перенос
результата в слой и порог; `app/layout/AppShell.test.tsx`:
панель `drawer` между шапкой и рабочей областью, её отсутствие у раздела без панели; backend —
`tests/test_spectral.py`
(мощность в свой ритм, топокарта как круг с прозрачностью, ETag/304, `None` вместо `NaN` в JSON,
роуты) и `tests/test_dipole_scanner.py` (сетка-шар, возврат синтетического диполя с точностью шага,
одна точка на эпоху, прогресс по эпохам, роуты)); срез 4 — таблица локализации
(`shared/lib/tableRows.test.ts`: все точки результата включая точки без MNI, «—» вместо нулей и
`NaN`, сортировка по эпохе в обе стороны, порядок строк одной эпохи, состав и видимость колонок;
`state/tableParams.test.ts`: направление сортировки, скрытие колонок, «Показать все»/сброс и
неприкосновенность результата расчёта; `table/LocalizationTableSection.test.tsx`: ноль запросов к
серверу, порядок строк по эпохе и его переворот настройкой, скрытая колонка не рисуется, «—» у
точки без MNI, порог «КД» строки не прячет, пустой результат объяснён; `table/LocalizationTablePanel.test.tsx`:
сортировка и колонки через состояние, справка по результату, предупреждение о несвежих параметрах;
`state/dipoleCalc.test.ts`: отпечаток параметров и сверка с результатом); срез 3.5 — доработки раздела
«Диполи» (`shared/lib/spectrum.test.ts`: окно частот — сортировка ввода, зажим в измеренные частоты,
срезка PSD без сдвига пар «частота–мощность», общий масштаб логарифма, попадание ритма в окно;
`shared/lib/dipolePoints.test.ts`: сила диполя с насыщением, фиксированное кольцо позиции и
оформление луча, наконечник от длины луча; `shared/lib/mriProjections.test.ts`: эллипсы полей Бродмана ровно там, где их ловит
`brodmannAreaAt`; `state/dipoleCalc.test.ts`: окно частот — просмотр без запросов, персист окна и не
персист выделения, выделение диполя переключателем и сброс вместе с результатом;
`dipoles/MriProjection.test.tsx`: раздельные слои позиций/векторов, наконечник полигоном, белое
кольцо фиксированного размера, клик по точке выделяет и наводит срезы;
`dipoles/DipolesSection.test.tsx`: подсветка во всех трёх проекциях, наведение срезов на позицию
выделенного диполя, отсутствие запросов, снятие выделения; `dipoles/DipolesDrawer.test.tsx`: сужение FFT
по ритму и полям «от/до», приглушение полос вне окна, пустое окно; `dipoles/DipolesPanel.test.tsx`:
отдельные чекбоксы позиций и векторов; срез 3.6 — форма фильтров расчёта
(`shared/lib/calcFilter.test.ts`: пресеты δ…γ из метаданных, нормализация полосы (порядок, округление,
зажим, «пустая» полоса = без фильтра), вывод пресета из полосы, одиночная частота `f ± bw/2`, сетевой
фильтр только 50/60 Гц, итог печатает полосу; `state/dipoleCalc.test.ts`: сеттеры полосы/notch с
зажимами, формы **обеих** задач с `band_min`/`band_max`/`notch_hz`, нормализация сохранённых параметров
и подъём состояния прежней версии UI; `dipoles/DipolesPanel.test.tsx`: ритмы из `/meta`, «свой
диапазон» не «отскакивает» к пресету при совпавших числах, одиночная частота, «без фильтра» + сетевой,
предупреждение «параметры расчёта изменены»; `dipoles/DipolesToolActions.test.tsx`: полоса и notch из
панели действительно уходят в задачу; backend `tests/test_filter_band_jobs.py`: задачи спектра и
диполей считают по присланной полосе (альфа не смешивается с δ/γ, узкая полоса `f ± bw/2` находит
точки) и возвращают её эхом результата); срез 3.7 — воспроизведение траектории
(`shared/lib/playback.test.ts`: скорости ×1/×2/×4 и приведение чужого значения, длительность и номер
эпохи с зажимом, доля внутри эпохи, пропуски отброшенных эпох, slerp направления с фолбэками,
интерполяция позиции/вектора/амплитуды и «измеренные» поля кадра, подпись кадра, шлейф: окно и
затухание, разрывы, порог «КД», предел числа отрезков; `state/dipoleCalc.test.ts`:
играет/пауза, play с последней эпохи, покадрово с зажимом и без сдвига счётчика команд, скорость, снятие
кадра вместе с результатом, не-персист; `dipoles/PlaybackFrame.test.tsx`: часы на управляемом `rAF` —
кадр на паузе равен измерению, ход эпох, ×2 вдвое быстрее, стоп в конце записи, команда «покадрово»,
стоп при уходе из раздела, порог «КД» на кадре, шлейф к кадру и его отсутствие без кадра, статичный
сосед не перерисовывается на кадрах;
`dipoles/MriProjection.test.tsx` и `dipoles/DipolesSection.test.tsx`: гало кадра, затухающий шлейф под
маркером и приглушение облака во всех трёх проекциях, уважение выключенных слоёв, ноль запросов;
`dipoles/DipolesToolActions.test.tsx`:
кнопки кадра, скорость, подпись кадра; `app/layout/AppShell.test.tsx`: `Space` только в разделе «Диполи»);
срез 3.8 (анатомия на срезах) — `shared/lib/atlasContours.test.ts` (квантование среза и версия в URL,
знаки осей, `evenodd`-дырки, приоритет мелкой метки над крупной, счётчик меток),
`dipoles/MriProjection.test.tsx` (полигоны структур и полей вместо фикстурных эллипсов, при `null`
эллипсы возвращаются, клик по полигону отдаёт структуру и поле, структура под курсором, выключенный
слой не рисуется), `dipoles/DipolesSection.test.tsx` (контуры по статическому URL с версией,
суммарный счётчик, «Структура под точкой», только статические запросы),
`dipoles/DipolesPanel.test.tsx` (слой структур отдельно от среза и полей),
`state/dipoleParams.test.ts` (структура под кликом и её сброс);
срез 3.9 (поправки) — `dipoles/DipolesToolActions.test.tsx` (справка открывается кнопкой, закрывается
Esc и кнопкой «Закрыть», ни одного запроса), `dipoles/DipolesSection.test.tsx` (в рабочей области нет
пояснительного абзаца; выключенный слой анимации убирает кадр и **не** приглушает облако),
`dipoles/DipolesPanel.test.tsx` (чекбокс «Кадр воспроизведения» отдельно от облака),
`dipoles/MriProjection.test.tsx` (кадр, его луч и шлейф живут в слое `layer-playback`, зависят только
от него, а не от слоёв позиций и векторов), `shared/lib/dipolePoints.test.ts` (кольцо Ø 6 px,
приглушение 0.2 × 1.5, структура в подписи точки), `table/LocalizationTableSection.test.tsx` (колонка
«Структура»: метка атласа из результата, «—» у точки без MNI, колонка скрывается),
`state/tableParams.test.ts` (состав колонок из 10 ключей); backend —
`tests/test_atlas_contours.py` (точка MNI → узел объёма с округлением «половина вверх», подпись
структуры и честный `None` без атласа, сверка с меткой реального тома) и
`tests/test_dipole_scanner.py` (поле `anatomical_structure` в точке).
Срез 5 (раздел «ЭЭГ») — `shared/lib/eegView.test.ts` (общая левая граница графика и столбец значений
у обеих половин, зажим разделителя по минимальным высотам, деления линеек мкВ/частот/времени,
перетаскивание линеек, окно времени как у вьюера EDF, клик по треку → уровень сигнала, рамка окна
трека: обводка окна внутри показанного, молчание при совпавших окнах и зажим по области графика),
`shared/lib/eegSpectrogram.test.ts` (разбор
контейнера `DPS2`, отбор строк/столбцов окна, перевод дБ в цвет, палитры и таблица цветов,
сглаживание по времени и частоте, детерминированная демо-сетка с α-пиком), `state/eegParams.test.ts`
(правки просмотра без запросов, равенство/расхождение отпечатков результата и параметров, форма
запроса, задача с поллингом и отдельной загрузкой сетки, ошибки, `eegNav`, сброс данных записи,
нормализация сохранённых параметров), `sections/eeg/EegSection.test.tsx` (пустое состояние без
записи, две половины и **одна** полоса времени, ноль запросов к спектрограмме при отрисовке, общий
курсор кликом по треку и по полосе времени, клик по спектрограмме даёт время и частоту и подтягивает
окно трека в «обзоре», клик по треку даёт уровень сигнала в полосе состояния и снимает частоту прежней
точки, «связано» окно не двигает, двойной клик снимает метки, высоты холстов половин,
перетаскивание правой линейки меняет мкВ/деление без запросов,
разделитель с клавиатуры и мышью), `sections/eeg/eegCanvas.test.ts` (масштаб холста из
`devicePixelRatio`, размеры bitmap и трансформация, `putImageDataAt` кладёт пиксели в bitmap-координатах
— `putImageData` не знает про трансформацию, маркер частоты и линия уровня: линия через область графика
и значение в столбце линеек, нулевая линия пунктиром, рамка окна трека: приглушение вне рамки, обводка
и подпись только на широкой рамке, вертикаль линейки на высоту холста в CSS-пикселях),
`sections/eeg/EegWindowControls.test.tsx` (уровень зума в свой
стор и отпечаток расчёта не меняется, листание выключено при ×1 и пишет `eegNav.seq`, один контрол с
EDF при разных сторах), `viewer/TrackStack.test.tsx` (переход в «ЭЭГ» по названию канала с явным
выбором канала), `registry.test.ts` / `AppShell.test.tsx` / `uiStore.test.ts` (шесть рабочих разделов,
порядок иконок рейла, хоткей `3` у «ЭЭГ», Space не перехватывается в «ЭЭГ»); backend — `tests/test_spectrogram.py` (тон 10 Гц
попадает в свою частотную строку, окно и перекрытие задают число столбцов, +20 дБ за ×10 амплитуды,
отсечение по верхней частоте, тексты ошибок вместо пустой сетки, round-trip контейнера `DPS2`,
подпись расчёта, кэш сетки, задача с прогрессом по окнам, ETag/304, 400/404). Этап 2 (17.09.2026) —
единый модуль кэша и кэш подготовленного сигнала: `tests/test_cache_store.py` (путь внутри `cache_dir`,
промах чтения как `None`, запись с созданием каталогов и публикацией через `os.replace`, сбой записи
без исключения и без обломков, очистка одной записи против целого вида кэша, запрет очистки без
частей пути) и `tests/test_prepared_signal.py` (повтор с теми же параметрами не читает EDF, другая
полоса/notch/референс — промах со своим фильтром, наружу уходит копия, аннотации `segment_epochs`
не «протекают» в соседний расчёт, LRU-лимит и вытеснение, выключенный кэш, очистка по записи и
вытеснением реестра). Этап 3 (17.09.2026) — тонкие роуты и единый ETag/304:
`tests/test_api_assets.py` (нормализация и разбор `If-None-Match` — список тегов, слабый тег,
`*`, отсутствие подстрочного совпадения `"v1"`↔`"v10"`; 200/304 с метаданными и без тела;
составная версия одним тегом; **сторож**: `status_code=304` не встречается вне `api/assets.py`),
`tests/test_api_params.py` (пара границ полосы и односторонняя полоса, разбор каналов референса,
длина эпохи из настроек, `single_freq` только с `freq_band='all'`, сборка `PreprocessParams` /
`SpectrumParams` / `DipoleScanParams` / `SpectrogramParams` с проверкой окна, параметры сетки из
результата задачи) и `tests/test_api_recording_jobs.py` (`result_url` рядом с записью для
`preprocess`/`spectrum`/`dipoles`/`spectrogram` против общего `/jobs/{id}/result`, пустой
`result_url` у идущей задачи, 404/409 чужого, незавершённого и упавшего результата, запуск
`POST /recordings/{id}/preprocess` с воркером из `WORKERS`: адреса в echo, meta и результат).
Этап 4 (17.09.2026) — единый поллинг задач и пары запросов клиента: `shared/lib/jobPolling.test.ts`
(опрос до `succeeded` с отдачей прогресса в `onTick`, пауза `JOB_POLL_MS` проверяется фейковыми
таймерами, ошибка задачи приходит текстом сервера, а без текста — понятной подстановкой, отмена
устаревшего запуска закрывает цикл без лишних запросов, семантика `createRunToken`: `next()` и
`cancel()` обесценивают прежнюю попытку). Отмена проверена и на уровне сторов отложенным ответом
(`test/deferred.ts`): `state/dipoleCalc.test.ts` — «Сбросить расчёт» прекращает опрос, прежний ответ
не возвращает результат; `state/edfRecording.test.ts` — «Закрыть запись» отменяет поллинг стадии,
прогресс прежней задачи не возвращается в состояние.
Этап 5 (17.09.2026) — журнал шагов: `tests/test_journal.py` (формат строки и все поля, прочерк `-`
в файле → `null` в API, запись при исключении с `error=…` в `note` и проброс ошибки, `job_scope`
возвращает прежний контекст, `JOURNAL_ENABLED=false`, сбой записи не ломает расчёт, битые и чужие
строки пропускаются, ротация на два поколения, фильтр по `pipeline` и `limit`, `cache_hit` у кэша
подготовленного сигнала, шаги задачи несут `job_id`, а синхронный вызов — прочерк; пустой журнал и
валидация `limit` у роута).
Этап 6 (17.09.2026) — жизненный цикл данных: обход сирот, отпечаток версий ассетов, сохранение
результатов задач. `tests/test_orphans.py` (7: кэш исчезнувшей записи сносится, а живой и записи,
чей каталог лежит на диске без сайдкара, — нет; ассеты и `journal.jsonl` не трогаются; устаревший
каталог загрузок удаляется, корневой файл остаётся; файлы задач исчезнувшей записи и история сверх
лимита; сбой реестра не роняет уборку; пустые каталоги). `tests/test_asset_versions.py` (8: снапшот
входов трёх ассетов — «забыли поднять версию» падает здесь; отпечаток — 16 hex; чувствительность к
версии, параметрам, файлам данных и их пропаже; три сервиса считают отпечаток одной функцией;
отпечатки ассетов различимы; опечатка в имени ассета — `KeyError`). `tests/test_job_store.py`
(8: задача после завершения пишется на диск и поднимается новым менеджером вместе с результатом,
`meta` и `elapsed_sec`; упавшая задача восстанавливается с текстом ошибки; слишком большой результат
не сохраняется — история остаётся, API отвечает «не сохранён»; `JOB_STORE_ENABLED=false`;
`prune_records` по исчезнувшей записи и по лимиту; битый и чужой по версии файл пропускаются; путь
файла защищён от `../`; `to_record`/`from_record` сохраняют `meta`).
Всего **564 теста Vitest (51 файл) и 327 pytest** (из них
26 — геометрия, укладка и кэш среза МРТ, 25 — контуры атласа (изолинии, выборка осей,
производная BA-разметка, структура по MNI, кэш, роуты), 14 — спектр и быстрый расчёт,
12 — спектрограмма канала, 10 — дедуп загрузок,
сайдкар записи и чистка каталога от копий, 16 — этап 2: единый кэш и подготовленный сигнал,
12 — этап 5: журнал шагов, 23 — этап 6: обход сирот, версии ассетов, файлы задач,
56 — этап 3: единый ETag/304, разбор форм, задачи записи и пайплайн в сервисе,
13 — этап 7: контракт точного фитинга (кортеж `fit_dipole`, сводка ошибок, кэш BEM, общая
анатомия с UI) и референс сетки спектрограммы в результате задачи, 9 — F21: строки `epochs` в БД
(`tests/test_analysis_db.py`: сборка записей эпох с отброшенными и порядком мощностей, настоящий FK
`dipoles.epoch_id` — join + `PRAGMA foreign_key_check`, `epoch_id` = id строки, а не номер эпохи,
старый результат без `epochs`, диполь с несуществующим `epoch_index`, общий PSD `compute_band_powers`
на синтетике). `mriProjections.test.ts`
дополнительно фиксирует знаки осей MNI, подписи краёв и единый масштаб проекций с пропорциями колонок
раздела, а фикстуры слоёв и опции трека вынесены в свои модули — у каждого свой тест:
`mriDemoShapes.test.ts` (детерминированность силуэта, поведение по глубине среза, согласие эллипсов
полей Бродмана с хит-тестом), `trackOptions.test.ts` (точность подписей оси времени, общий и
поканальный диапазон, опции чарта: ось времени только у нижнего трека, band огибающей).
**Правило:** новый сервис/багфикс → тест (backend → pytest, frontend → Vitest).

> Перенесено дословно из прежнего `AGENTS.md` (разделы: «Тесты»).
> Правьте правила здесь; `AGENTS.md` держит только ссылку на этот файл.
> Правило ведения документации — `docs/rules/docs.md`.
>
> `AGENTS.md` держит только краткую выжимку (команды и правило «новый сервис → тест»):
> полный перечень — здесь, чтобы рефакторинг не терял знание о покрытии.
