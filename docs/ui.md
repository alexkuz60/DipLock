# DipLock UI — функциональная спецификация (индекс)

> **Статус:** Фаза 0 (контракт API) и Фаза 1 (каркас UI) реализованы 13.09.2026;
> срезы 2.0 (контролы), 2.1 (параметры раздела EDF), 2.2 (загрузка записи),
> 2.3 (вьюер треков), 2.4 (тулс-хедер раздела EDF: действия, стадии перерасчёта, паспорт,
> комбо-бокс зума), 2.5 (реальные сигналы записи), 2.6 (слои артефактов/эпох),
> 2.7 (стадии предподготовки), 2.8 (экспорт окна), 2.9 (ревизия раздела EDF:
> паспорт и «Закрыть запись» в шапке, навигация по окну, курсор по клику, разворот трека,
> неотключаемый скроллбар) и 2.10 (правки по итогам ручного просмотра: скроллы рабочих
> областей, разметка эпох по сетке результата, ручные пометки эпох Ctrl+двойным кликом)
> и 2.11 (оверлеи вьюера внутри прокручиваемого контента, широкая полоса прокрутки,
> счётчик пометок) — готовы 14.09.2026.
> Связанные документы: `README.md`, `AGENTS.md`, `audit.md` (§7 — находки F1–F16, §7.7 — F17–F21),
> `frontend/README.md` (команды и структура кода).

## 1. Принципы и ограничения

| Принцип | Реализация |
|---|---|
| Только десктоп, без мобильного адаптива | `min-width: 1366px` на `body`, `height: 100vh`, скролл только внутри рабочих областей |
| Тёмная тема | Токены Tailwind v4 в `frontend/src/styles/index.css` (`--color-bg-*`, `--color-fg-*`, `--color-accent`) |
| Крупный шрифт, системный для ОС | `system-ui, -apple-system, "Segoe UI", Roboto, Ubuntu…`; базовый кегль 16/18/20 px (переключатель в «Настройках»), цифры — `tabular-nums` |
| Крупные иконочные кнопки с тултипами | `IconButton` + Radix `Tooltip` (задержка 300 мс, доступ по фокусу с клавиатуры, есть `aria-label`) |
| Три зоны | Левый рейл 72 px (навигация) · центр: тулс-хедер + рабочая область + статусбар · правый сайдбар 384 px (схлопывается в 48 px) |
| Хоткеи | `1…6` — разделы, `[` — панель опций; обработчик игнорирует ввод в полях |
| **Обработка — только по кнопке** | Правка параметра никогда не запускает расчёт: параметры складываются в состояние раздела, кнопка действия становится доступной, UI показывает «параметры изменены — результат не пересчитан». Расчёт стартует исключительно по нажатию кнопки: сначала параметры и визуализация, обработка подключается к уже готовому UI |
| Долгие операции | Только через job-API (`POST /api/v1/jobs`), прогресс по этапам, индикатор активных задач в рейле; перед запуском дорогой операции показывается оценка стоимости |

## 2. Карта экранов

| # | Раздел | Назначение | Тулс-хедер | Правая панель | Статус |
|---|---|---|---|---|---|
| 1 | **Главная** (`/`) | Заставка-название по центру, строка готовности, быстрые действия | нет | нет | готово |
| 2 | **EDF** (`/edf`) | Просмотр записи: треки ЭЭГ, зоны артефактов, границы эпох, предподготовка | да (действия, стадии, паспорт, листание окна, зум, слои) | да | действия шапки + вьюер на реальных сигналах, стадии и слои из результата (срезы 2.2–2.11) |
| 3 | **ЭЭГ** (`/eeg`) | Один канал: сырой трек сверху, спектрограмма снизу, общая ось и курсор, метки точки клика | да (канал, расчёт, зум/листание, справка) | да | трек на реальных сигналах, спектрограмма STFT по кнопке, линейки значений, нулевая линия и линия уровня, рамка окна трека в «обзоре», разделитель, режимы «связано/обзор», переход из EDF по названию канала (срез 5), виртуальные каналы-миксы (все каналы, полушария, области) и слои артефактов из EDF на обеих половинах (срез 5+) |
| 4 | **Диполи** (`/dipoles`) | Запуск расчёта, 3 проекции мозга, playback траектории | да | да | три проекции (SVG) со слоями, срезы кликом и линейкой, точка/перекрестие (срез 3.1), реальный срез МРТ (3.2), быстрый расчёт по кнопке и спектр в выдвижной панели (3.4); точный профиль и playback — далее |
| 5 | **Таблица** (`/table`) | Таблица локализации (MNI, амплитуда, GOF, BA), экспорт | да | да | все точки результата, сортировка по эпохе и состав колонок (срез 4); фильтры, виртуализация и экспорт — далее |
| 6 | **Групповой анализ** (`/group`) | Записи из БД: групповые фильтры, агрегаты по BA, поиск закономерностей | да | да | каркас + заглушка |
| ⚙ | **Настройки** (`/settings`) | Масштаб текста, плотность, параметры расчёта (read-only из `/meta`) | да | нет | готово |
| 🖥 | **Состояние сервера** (`/server`) | Проверки готовности, версии, пути, параметры расчёта, поллинг 5 с | да | нет | готово |

Раздел 6 (групповой анализ) пока показывает плейсхолдер с планом работ — это осознанный
шаблон: новый раздел = запись в `frontend/src/app/sections/registry.ts` + компонент в `routes.tsx`.

## 3. Разделы подробно (целевой функционал)

## Индекс спецификации

Спецификация разрезана по разделам (17.09.2026), номера § сохранены: на них ссылаются код,
тесты и комментарии (`docs/ui.md §3.3`, `§8`, `§9`, `§12`).

| § | Раздел | Файл |
|---|--------|------|
| 1–2 | Принципы, карта экранов | `docs/ui.md` (этот файл) |
| 3.1 | Главная | `docs/ui/shell.md` |
| 3.2 | EDF — просмотр записи и подготовка | `docs/ui/viewer.md` |
| 3.3 | Диполи — расчёт и 3 проекции | `docs/ui/dipoles.md` |
| 3.4–3.5 | Таблица локализации, групповой анализ | `docs/ui/table.md` |
| 3.6–3.7 | Настройки, состояние сервера | `docs/ui/shell.md` |
| 3.8 | ЭЭГ — один канал: трек и спектрограмма | `docs/ui/eeg.md` |
| 4–7, 9 | Стек, структура `frontend/`, запуск, контракт API, хоткеи | `docs/ui/shell.md` |
| 8 | Пирамида зума ЭЭГ | `docs/ui/viewer.md` |
| 10–12 | Дорожная карта, решения и открытые вопросы, компромиссы | `docs/ui/roadmap.md` |

> Разрезана 17.09.2026: разделы живут в `docs/ui/*.md`, здесь — принципы, карта экранов
> и индекс. Новый раздел спецификации = файл в `docs/ui/` + строка в таблице выше.
