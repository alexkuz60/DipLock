fastapi>=0.115.0
uvicorn[standard]>=0.30.0
mne>=1.7.0
# Топокарты (N32): mne.viz.plot_topomap рендерит в Agg. matplotlib уже шёл
# транзитивной зависимостью MNE, но стал прямым runtime-импортом
# (app/services/spectral.py) — объявляем явно, как joblib/sklearn (F3).
matplotlib>=3.8.0
numpy>=2.0.0
scipy>=1.14.0
nibabel>=5.0.0
# SQLAlchemy asyncio-слой (движок создаётся на импорте в app/models/db.py) требует
# greenlet — без него ImportError при старте и при сборе тестов БД. Голый `sqlalchemy`
# greenlet не тянет, поэтому заявляем extra (случай 26.09.2026, как F3 с aiosqlite).
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
# Асинхронный драйвер SQLite: локальный DATABASE_URL=sqlite+aiosqlite://… (движок
# создаётся на импорте в app/models/db.py) — без него падает ModuleNotFoundError (F3).
aiosqlite>=0.20.0
alembic>=1.13.0
psycopg2-binary>=2.9.0
python-multipart>=0.0.10
pydantic-settings>=2.5.0
trimesh>=4.5.0
# Децимация surface-мешей (trimesh.Trimesh.simplify_quadric_decimation)
fast-simplification>=0.1.7
# Параллелизм mne.fit_dipole (n_jobs=-1): опциональная зависимость MNE — без неё
# он молча считает в один поток, и `DIPOLE_REFINE_N_JOBS=-1` ничего не обещает.
joblib>=1.4.0
# ICA (детекция EOG-компонент и очистка `ica.apply`): метод fastica MNE требует
# sklearn — без него `ica.fit` падает ImportError (N8, шаг 2.1).
scikit-learn>=1.5.0
# Разложение 1/f + пики спектра (specparam/FOOOF, шаг 2.4): апериодика и
# гауссовые пики над ней. Стабильного 2.0 ещё нет — на PyPI только rc-сборки,
# API зафиксирован тестами (`tests/test_spectral.py`), как у MNE.
specparam>=2.0.0rc7
